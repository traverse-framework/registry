[workspace]

[package]
name = "uncertainty-score-agent"
version = "1.0.0"
edition = "2015"
publish = false
description = "Callweave-owned source mirror of uncertainty.score for independently measured registry coverage."

[[bin]]
name = "uncertainty-score-agent"
path = "src/main.rs"

[profile.release]
opt-level = "s"
lto = true
panic = "abort"
