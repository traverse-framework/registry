[workspace]

[package]
name = "core-normalize-participants-agent"
version = "0.1.0"
edition = "2021"
publish = false
description = "Catalog-source mirror of core.normalize-participants for measured llvm-cov coverage (logic from traverse examples/core-normalize-participants)."

[[bin]]
name = "core-normalize-participants-agent"
path = "src/main.rs"

[profile.release]
opt-level = "s"
lto = true
panic = "abort"
