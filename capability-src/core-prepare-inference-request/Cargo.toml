[workspace]

[package]
name = "core-prepare-inference-request"
version = "1.0.0"
edition = "2021"
publish = false

[[bin]]
name = "core-prepare-inference-request"
path = "src/main.rs"

[dependencies]
wasi-capability-runtime = { path = "../wasi-capability-runtime" }

[profile.release]
opt-level = "s"
lto = true
panic = "abort"
