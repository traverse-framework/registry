[workspace]

[package]
name = "audio-pcm-window"
version = "1.0.0"
edition = "2021"
publish = false
description = "Bounded deterministic PCM window extraction."

[[bin]]
name = "audio-pcm-window"
path = "src/main.rs"

[profile.release]
opt-level = "s"
lto = true
panic = "abort"
