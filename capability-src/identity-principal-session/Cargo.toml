[workspace]

[package]
name = "identity-principal-session-agent"
version = "0.1.0"
edition = "2021"
publish = false
description = "Catalog-source mirror of identity.principal-session — durable authenticated session via host state."

[[bin]]
name = "identity-principal-session-agent"
path = "src/main.rs"

[profile.release]
opt-level = "s"
lto = true
panic = "abort"
