[workspace]

[package]
name = "core-aggregate-team-action-health-agent"
version = "0.1.0"
edition = "2021"
publish = false
description = "Catalog-source mirror of core.aggregate-team-action-health for measured llvm-cov coverage (logic from traverse examples/core-aggregate-team-action-health)."

[[bin]]
name = "core-aggregate-team-action-health-agent"
path = "src/main.rs"

[profile.release]
opt-level = "s"
lto = true
panic = "abort"
