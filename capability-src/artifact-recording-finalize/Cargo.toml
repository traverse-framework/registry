[workspace]

[package]
name = "artifact-recording-finalize-agent"
version = "1.0.0"
edition = "2015"
publish = false
description = "Callweave-owned source mirror of artifact.recording-finalize for independently measured registry coverage."

[[bin]]
name = "artifact-recording-finalize-agent"
path = "src/main.rs"

[profile.release]
opt-level = "s"
lto = true
panic = "abort"
