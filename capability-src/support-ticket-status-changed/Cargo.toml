[workspace]

[package]
name = "support-ticket-status-changed-agent"
version = "0.1.0"
edition = "2021"
publish = false
description = "Catalog-source mirror of support.ticket-status-changed — ticket status state machine with governed emit."

[[bin]]
name = "support-ticket-status-changed-agent"
path = "src/main.rs"

[profile.release]
opt-level = "s"
lto = true
panic = "abort"
