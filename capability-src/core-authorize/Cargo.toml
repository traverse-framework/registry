[workspace]

[package]
name = "core-authorize-capability"
version = "0.1.0"
edition = "2021"
publish = false
description = "Source mirror for core-authorize coverage."

[[bin]]
name = "core-authorize-capability"
path = "src/main.rs"

[profile.release]
opt-level = "s"
lto = true
panic = "abort"
