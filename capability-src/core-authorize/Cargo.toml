[workspace]

[package]
name = "core-authorize-agent"
version = "0.1.0"
edition = "2021"
publish = false
description = "Catalog-source mirror of core.authorize for measured llvm-cov coverage (logic from traverse examples/core-authorize)."

[[bin]]
name = "core-authorize-agent"
path = "src/main.rs"

[profile.release]
opt-level = "s"
lto = true
panic = "abort"
