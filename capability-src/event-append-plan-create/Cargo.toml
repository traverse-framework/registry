[workspace]

[package]
name = "event-append-plan-create"
version = "1.0.0"
edition = "2021"
publish = false
description = "Pure bounded append-envelope planning for event-store connectors."

[[bin]]
name = "event-append-plan-create"
path = "src/main.rs"

[profile.release]
opt-level = "s"
lto = true
panic = "abort"
