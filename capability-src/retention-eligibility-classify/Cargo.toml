[workspace]

[package]
name = "retention-eligibility-classify-agent"
version = "1.0.0"
edition = "2015"
publish = false
description = "Callweave-owned source mirror of retention.eligibility-classify for independently measured registry coverage."

[[bin]]
name = "retention-eligibility-classify-agent"
path = "src/main.rs"

[profile.release]
opt-level = "s"
lto = true
panic = "abort"
