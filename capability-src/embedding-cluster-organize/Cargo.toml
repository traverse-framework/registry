[workspace]

[package]
name = "embedding-cluster-organize-agent"
version = "1.0.0"
edition = "2015"
publish = false
description = "Callweave-owned source mirror of embedding.cluster-organize for independently measured registry coverage."

[[bin]]
name = "embedding-cluster-organize-agent"
path = "src/main.rs"

[profile.release]
opt-level = "s"
lto = true
panic = "abort"
