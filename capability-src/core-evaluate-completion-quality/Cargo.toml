[workspace]

[package]
name = "core-evaluate-completion-quality-agent"
version = "0.1.0"
edition = "2021"
publish = false
description = "Catalog-source mirror of core.evaluate-completion-quality for measured llvm-cov coverage (logic from traverse examples/core-evaluate-completion-quality)."

[[bin]]
name = "core-evaluate-completion-quality-agent"
path = "src/main.rs"

[profile.release]
opt-level = "s"
lto = true
panic = "abort"
