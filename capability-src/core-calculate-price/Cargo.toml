[workspace]

[package]
name = "core-calculate-price-agent"
version = "0.1.0"
edition = "2021"
publish = false
description = "Catalog-source mirror of core.calculate-price for measured llvm-cov coverage (logic from traverse examples/core-calculate-price)."

[[bin]]
name = "core-calculate-price-agent"
path = "src/main.rs"

[profile.release]
opt-level = "s"
lto = true
panic = "abort"
