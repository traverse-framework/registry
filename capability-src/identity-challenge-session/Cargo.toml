[workspace]

[package]
name = "identity-challenge-session-agent"
version = "0.1.0"
edition = "2021"
publish = false
description = "Stateful identity.challenge-session host state_* capability."

[[bin]]
name = "identity-challenge-session-agent"
path = "src/main.rs"

[profile.release]
opt-level = "s"
lto = true
panic = "abort"
