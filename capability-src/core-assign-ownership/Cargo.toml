[workspace]

[package]
name = "core-assign-ownership-agent"
version = "0.1.0"
edition = "2021"
publish = false
description = "Catalog-source mirror of core.assign-ownership for measured llvm-cov coverage (logic from traverse examples/core-assign-ownership)."

[[bin]]
name = "core-assign-ownership-agent"
path = "src/main.rs"

[profile.release]
opt-level = "s"
lto = true
panic = "abort"
