[workspace]

[package]
name = "location-initialize-agent"
version = "1.0.0"
edition = "2015"
publish = false
description = "Callweave-owned source mirror of location.initialize for independently measured registry coverage."

[[bin]]
name = "location-initialize-agent"
path = "src/main.rs"

[profile.release]
opt-level = "s"
lto = true
panic = "abort"
