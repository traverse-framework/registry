[workspace]

[package]
name = "operations-recover-agent"
version = "1.0.0"
edition = "2015"
publish = false
description = "Callweave-owned source mirror of operations.recover for independently measured registry coverage."

[[bin]]
name = "operations-recover-agent"
path = "src/main.rs"

[profile.release]
opt-level = "s"
lto = true
panic = "abort"
