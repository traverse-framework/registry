[workspace]

[package]
name = "visual-parameter-map-agent"
version = "1.0.0"
edition = "2015"
publish = false
description = "Callweave-owned source mirror of visual.parameter-map for independently measured registry coverage."

[[bin]]
name = "visual-parameter-map-agent"
path = "src/main.rs"

[profile.release]
opt-level = "s"
lto = true
panic = "abort"
