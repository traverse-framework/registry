[workspace]

[package]
name = "commerce-pricing-config-agent"
version = "0.1.0"
edition = "2021"
publish = false
description = "Catalog-source mirror of commerce.pricing-config — durable pricing config via host state."

[[bin]]
name = "commerce-pricing-config-agent"
path = "src/main.rs"

[profile.release]
opt-level = "s"
lto = true
panic = "abort"
