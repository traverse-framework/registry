[workspace]

[package]
name = "approval-decision-apply-agent"
version = "1.0.0"
edition = "2015"
publish = false
description = "Callweave-owned source mirror of approval.decision-apply for independently measured registry coverage."

[[bin]]
name = "approval-decision-apply-agent"
path = "src/main.rs"

[profile.release]
opt-level = "s"
lto = true
panic = "abort"
