[workspace]

[package]
name = "report-summarize-agent"
version = "0.1.0"
edition = "2021"
publish = false
description = "Deterministic extractive summarizer for the report.* chain. See registry#429 / #426."

[[bin]]
name = "summarize-agent"
path = "src/main.rs"

[dependencies]
wasi-capability-runtime = { path = "../wasi-capability-runtime" }

[profile.release]
opt-level = "s"
lto = true
panic = "abort"
