[workspace]

[package]
name = "commerce-cart-line-changed-agent"
version = "0.1.0"
edition = "2021"
publish = false
description = "Catalog-source mirror of commerce.cart-line-changed — cart line qty change with governed emit."

[[bin]]
name = "commerce-cart-line-changed-agent"
path = "src/main.rs"

[profile.release]
opt-level = "s"
lto = true
panic = "abort"
