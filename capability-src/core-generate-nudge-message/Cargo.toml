[workspace]

[package]
name = "core-generate-nudge-message-agent"
version = "0.1.0"
edition = "2021"
publish = false
description = "Catalog-source mirror of core.generate-nudge-message for measured llvm-cov coverage (logic from traverse examples/core-generate-nudge-message)."

[[bin]]
name = "core-generate-nudge-message-agent"
path = "src/main.rs"

[profile.release]
opt-level = "s"
lto = true
panic = "abort"
