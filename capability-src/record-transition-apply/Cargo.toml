[workspace]

[package]
name = "record-transition-apply-agent"
version = "1.0.0"
edition = "2015"
publish = false
description = "Callweave-owned source mirror of record.transition-apply for independently measured registry coverage."

[[bin]]
name = "record-transition-apply-agent"
path = "src/main.rs"

[profile.release]
opt-level = "s"
lto = true
panic = "abort"
