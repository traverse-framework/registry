[workspace]

[package]
name = "core-validate-action-item-agent"
version = "0.1.0"
edition = "2021"
publish = false
description = "Catalog-source mirror of core.validate-action-item for measured llvm-cov coverage (logic from traverse examples/core-validate-action-item)."

[[bin]]
name = "core-validate-action-item-agent"
path = "src/main.rs"

[profile.release]
opt-level = "s"
lto = true
panic = "abort"
