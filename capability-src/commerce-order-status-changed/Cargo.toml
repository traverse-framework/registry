[workspace]

[package]
name = "commerce-order-status-changed-agent"
version = "0.1.0"
edition = "2021"
publish = false
description = "Catalog-source mirror of commerce.order-status-changed — order status transition with governed emit."

[[bin]]
name = "commerce-order-status-changed-agent"
path = "src/main.rs"

[profile.release]
opt-level = "s"
lto = true
panic = "abort"
