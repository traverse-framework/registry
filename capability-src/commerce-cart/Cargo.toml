[workspace]

[package]
name = "commerce-cart-agent"
version = "0.1.0"
edition = "2021"
publish = false
description = "Catalog-source mirror of commerce.cart — durable cart via host state."

[[bin]]
name = "commerce-cart-agent"
path = "src/main.rs"

[profile.release]
opt-level = "s"
lto = true
panic = "abort"
