[workspace]

[package]
name = "core-record-nudge-event-agent"
version = "0.1.0"
edition = "2021"
publish = false
description = "Catalog-source mirror of core.record-nudge-event for measured llvm-cov coverage (logic from traverse examples/core-record-nudge-event)."

[[bin]]
name = "core-record-nudge-event-agent"
path = "src/main.rs"

[profile.release]
opt-level = "s"
lto = true
panic = "abort"
