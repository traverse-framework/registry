# Registry Design Decision Log

Status: historical record of the brainstorm that shaped `specs/001-registry-foundation/spec.md`. This document is not itself governing — if it and the spec disagree, the spec wins. Kept for provenance: why the shape is what it is, not just what the shape is.

Date: 2026-07-03

## Context

Traverse (the runtime) already had an in-repo registry engine (`crates/traverse-registry` in `traverse-framework/traverse`) covering public/private overlay resolution, semver rules, dependency locking, connector registration, and composability metadata (governed by that repo's specs 005, 007, 011, 034–037, 039, 041, 043). That engine already assumed a "public registry" existed as a data source, but nothing actually served that role. This log captures the decisions made to stand up that role as a new, dedicated repo: `traverse-framework/registry`.

## Decisions

1. **Split boundary**: `traverse-contracts` stays in `traverse-framework/traverse`; `traverse-registry` (the crate, plus all capability content) moves to this repo. Rationale: `traverse-contracts` is the schema every consumer (runtime, CLI, MCP, this registry) depends on symmetrically — one canonical home avoids two repos both claiming to define "the schema." See `traverse/specs/051-registry-extraction/spec.md` for the formal migration record.

2. **CLA**: required org-wide, starting now (before this repo or `traverse` accumulates outside contributors), to preserve the option to relicense or commercially license the codebase later without needing retroactive consent from every contributor. Governed centrally in `traverse-framework/.github`.

3. **Architecture — git-based static index, not a hosted service, for v1**: capability records are files in this repo, validated and quality-gated by CI, merged via reviewed PRs — the same shape as crates.io's index repo or Homebrew-core. Chosen because: (a) it's free to run (GitHub Actions is free for public repos — no server, no database, no bill regardless of usage), and (b) it fits the existing spec-gated PR governance model this org already uses. Explicitly designed so a hosted API layer can be added later without a breaking schema migration — the resolve/fetch interface and the record schema are meant to be storage-agnostic from day one.

4. **Future hosted layer, when built**: serverless/edge, pay-per-use. Read path (resolving/fetching capabilities — the overwhelming majority of traffic in any registry) sits behind a CDN/edge cache; write path (publishing) runs on pay-per-request compute; artifact storage uses cheap object storage. The goal: running cost scales with actual usage, never a fixed bill for idle capacity.

5. **Schema reserves `owner` and `namespace` fields now**, defaulted to a "core" identity and an unscoped namespace, even though the only accepted publisher in v1 is the core team via PR review. Rationale: adding real third-party/dev publishing later without these fields would require a breaking migration of every existing record; reserving them now costs almost nothing.

6. **Sync/consume model**: `traverse-cli registry sync` (CLI, lives in `traverse-framework/traverse`) fetches the latest published index into local durable workspace state. The runtime (`traverse-registry` crate, wherever it ends up being consumed from) only ever resolves against that local synced state — zero live network dependency at execution time. This reuses the exact pattern `traverse`'s spec 046 already established for app registration (CLI writes durable local state, runtime reads it, never calls a live service).

7. **Publish mechanism**: merging a PR to `main` in this repo triggers CI to build a versioned aggregated index artifact, published as a GitHub Release. `sync` fetches that artifact, not the raw git tree. This reuses the shape of `traverse`'s existing spec 048 (tag → build → publish pipeline for its own crates), rather than inventing a new mechanism.

8. **Quality gate — deterministic + AI-advisory, not AI-blocking**: schema validation, semver-bump-vs-actual-diff checks, digest integrity, namespace-collision checks, and dependency-resolvability checks are deterministic and block merge. A separate AI pass flags likely semantic duplicates (capabilities that overlap in effect but not in name/tags) and capability-boundary quality issues (e.g. a CRUD wrapper disguised as a capability) as PR comments — advisory only. This is required to be advisory, not blocking, because the constitution (Principle II, inherited from `traverse-framework/.github`) already requires explicit manual approval before any contract publication; the AI pass informs that human review, it doesn't replace it.

9. **CLI automates the PR flow**: `traverse-cli capability publish` (implemented in `traverse-framework/traverse`, not here) validates a contract locally, computes the artifact digest, and uses git + `gh` to open a PR against this repo automatically — the equivalent of Homebrew's `brew bump-formula-pr`. The human review/approval gate on the PR is unchanged; only the mechanical git steps are automated.

10. **Repo layout**: `capabilities/<namespace>/<id>/<version>/contract.json` (+ artifact references alongside). Path-encodes identity so two capabilities structurally cannot occupy the same path (namespace collisions become impossible, not just checked), and a new version is always a new file — the filesystem itself enforces the immutability rule already required by `traverse`'s spec 005, instead of relying purely on CI logic to catch violations.

11. **Artifact storage**: WASM binaries are **not** committed into this git repo directly (git handles binary blobs and unbounded history growth badly, which actively fights an append-only immutable-version model). They live as GitHub Release assets; the git-tracked contract stores only a digest + release URL. Free to host, keeps the repo itself small and fast indefinitely.

12. **Deprecation**: a `cargo yank`-style mechanism — a version is never edited or deleted, but a separate deprecation-flag file marks it excluded from range resolution (`^1.2.0`-style) while still resolvable by an exact pin. This is the standard, well-proven shape every major package registry has converged on for exactly this problem, and it doesn't touch immutability.

13. **Tracking**: this repo gets its own GitHub Project board (Project 3, "Registry" — already existed, needed cleanup of 7 stray unrelated `traverse` issues that had been swept in incorrectly).

14. **Semver rules**: inherited as-is from `traverse`'s specs 005/037/043, moving with the crate rather than being redesigned.

15. **Repo naming across the org** (`traverse`, `reference-apps`, `website`, `registry` — lowercase-kebab-case, matching the dominant OSS convention and the org's existing crate-naming style) — decided in the same session, but tracked separately in `traverse/docs/repo-migration-plan.md`, not part of this repo's scope.

16. **Governance consolidation**: constitution, NFRs, quality standards, antipatterns, compatibility policy, exception process, CLA, and the spec-alignment CI script were pulled out of `traverse-framework/traverse` into a new shared repo, `traverse-framework/.github`, rather than copied into this repo. Every repo (including this one) keeps a thin pointer to a pinned governance version instead of a duplicated copy — see `.specify/memory/constitution.md` in this repo.

17. **AI-advisory review tooling refinement, then reverted (see 18)**: briefly switched the pass to a `.claude/skills/capability-review` Claude Code skill invoked via `claude -p` in CI, authenticated with a subscription token (`CLAUDE_CODE_OAUTH_TOKEN`, from `claude setup-token`) instead of a metered `ANTHROPIC_API_KEY`. The intent was avoiding per-call API billing when the org already carries a Claude subscription.

18. **Reverted decision 17 — subscription auth shares a real quota with personal usage**: `CLAUDE_CODE_OAUTH_TOKEN` authenticates as the account's actual Claude subscription, and Pro/Max/Team plans share **one unified usage limit across all Claude Code access** — interactive terminal sessions, IDE usage, and any CI job using that same token all draw from the same pool. For the repo owner, who actively depends on that plan for real interactive work, this meant CI runs could plausibly push personal usage into hitting plan limits sooner, or during, active interactive sessions. Reverted back to the original metered `ANTHROPIC_API_KEY` (Claude Console workspace key) design: costs isolated, billed separately, zero risk of touching personal usage. The `capability-review` skill file was removed since it's not used by the reverted implementation; the prompt logic lives inline in `scripts/ci/ai_advisory_review.py` instead. Spec 004's FR-001–004 are unaffected by this reversal, same as decision 17 — no normative requirement changed either way.

19. **`ANTHROPIC_API_KEY` deliberately not provisioned (as of 2026-07)**: after reverting decision 17, the repo owner explicitly said they don't want to allocate budget for a metered `ANTHROPIC_API_KEY` right now. The `ai-advisory-review` CI job is left wired up and correct (see `scripts/ci/ai_advisory_review.py`) but runs dormant: with no secret configured, `call_claude` raises immediately and every PR touching `capabilities/**/contract.json` gets the `DEGRADED_COMMENT` advisory notice instead of a real review. This is expected, not a bug -- do not "fix" it by requesting a key, by reintroducing subscription-token auth (see decision 18 for why that was reverted), or by making the job block merges to compensate. It stays this way until the repo owner decides to fund it.

20. **Bundle `scope: public/private` reconciled with this registry (2026-07-06)**: the pre-existing `traverse-registry` crate concept (traverse spec 005 FR-006/007: a public tier plus a private/local overlay with `PreferPrivate` lookup precedence, keyed as `(scope, id, version)`) and this repo's `namespace`/`owner` fields are **orthogonal axes that both stay** — resolution tier vs publisher identity. What was pinned down: **the public tier of any runtime's local state is populated only by `traverse-cli registry sync` from this repo's published index; "public" means "published in `traverse-framework/registry`," nothing else.** Consequences (normative traverse-side, tracked in `traverse-framework/Traverse#548`): `scope: private` keeps overlay behavior and additionally becomes a publish-refusal latch (`capability publish` refuses private-bundle content, npm `"private": true`-style); local `bundle register` with `scope: public` is rejected once sync exists (dev/testing is fully served by the private overlay, which already wins lookup — the cargo answer, where "inserting into your local copy of the crates.io index" isn't an operation that exists); a private-overlay record shadowing a synced public identity is allowed with a warning (cargo `[patch]`-style). **Why**: a survey of npm, cargo/crates.io, Go modules, and Docker/OCI showed no mature ecosystem lets an artifact author declare "I am public" as routing metadata — publicness is always a fact about the source a record came from, and the npm/PyPI dependency-confusion attack class is exactly what leaving two writers of one public namespace enables. The alternative options — leaving the two meanings of "public" unreconciled, or replacing declared `scope` with derived provenance — were rejected respectively as "guaranteed `ImmutableVersionConflict` at sync time" and "a breaking manifest-schema migration that buys nothing Option A doesn't." Team-shared private registries (multi-source sync with per-namespace routing, npm-`.npmrc`-style) were explicitly deferred but designed-for: the single-source rule generalizes to a source list additively. Registry-side companion: `specs/006-public-scope-and-identity` (Draft).

21. **Consumer-side resolution goes dual-mode; artifacts fetched at registration time (2026-07-06)**: once `registry sync` (traverse #542) exists, component manifests support two mutually exclusive capability reference forms — the existing `contract_path` (+ `wasm_binary_path`/`wasm_digest`), which keeps meaning "this component bundles its own capability" and registers it into the **private overlay** (consistent with reference-apps' existing `workspace_defaults.registry_scope: "private"`), and a new `registry_ref: {namespace, id, version_range}` resolved from the sync-populated public tier. **Why dual-mode**: it is the convergent prior-art shape (cargo path-vs-registry dependencies, Go `replace`, npm `file:`), it is purely additive (no existing manifest breaks), and the rejected alternatives both broke the app-local development model — registry-only references would force publishing before an app could even register its own capability, and a separate override file adds indirection to what is today the common case. **Artifact fetch timing**: `sync` fetches `index.json` only; WASM artifacts download at app-registration time into a local content-addressed, digest-verified cache. This keeps sync cheap (bandwidth scales with what you use, not with the registry), while preserving the constitutional zero-live-network-at-execution rule — registration is a CLI-time operation, execution never touches the network. Normative home: the traverse-side spec under `traverse-framework/Traverse#548`; reference-apps adoption tracked as `reference-apps#97`, gated on that work plus seed content.

22. **Seed content: `traverse-starter.process` 1.0.0, published as-is via manual PR; artifacts hosted in this repo (2026-07-06)**: the registry's first real content is the one genuine capability contract the org has (`trace-explorer` turned out to have no contracts at all — it is a React client only), published unchanged: namespace `traverse-starter`, existing `owner` object, existing digest. **Why as-is**: re-namespacing to `core` would break identity continuity (when reference-apps later flips to `registry_ref`, the identity it references should be the one it already uses) and would skip exercising the non-core namespace path; a synthetic hello-registry capability would prove the pipeline on throwaway content when real content exists. **Why a manual PR, before the #543 CLI**: the CLI only automates git mechanics, so publishing without it proves the pipeline is CLI-independent and gives #542/#543 real content to build and test against — seeding first inverts the dependency the right way. **Why artifacts live in this repo's releases** (tag scheme `artifacts/<id>-<version>`): this repo owns the immutability guarantee, so a published record's integrity must not depend on another repository's release hygiene; the wasm is uploaded as a release asset here and the published contract copy gains the `artifact: {digest, url}` field (the source contract in traverse stays untouched — the registry copy is the publication record). Governed by `specs/007-artifact-hosting` (Draft), which also closes the "which release, uploaded when, by whom" gap spec 001 FR-007 left open.

23. **Draft specs are tracked in a `draft_specs` array, not in `specs[]` (2026-07-06)**: the session's ground rule was "new specs enter `approved-specs.json` as Draft; approval is the repo owner's explicit, standalone act." The vendored spec-alignment gate, however, fails CI if **any** `specs[]` entry has `status != approved` (it validates every record, not just declared ones). Rather than forking the vendored governance script (contradicting decision 16) or leaving drafts untracked in the governance file, drafts are registered in a separate top-level `draft_specs` array in the same file — the gate reads only `schema_version` and `specs`, so drafts are visible and machine-readable without breaking CI. Approval = the repo owner (never an agent) moving the record into `specs[]` with `status: approved` / `immutable: true` and the `will_govern` list becoming `governs`. If governance later standardizes a draft-tracking convention upstream in `traverse-framework/.github`, this repo adopts it and drops the local convention.

24. **Co-brainstormed specs are approved by the brainstorm itself (2026-07-06)**: the repo owner stated that specs produced from a joint brainstorm session with the owner do not require a separate, standalone sign-off act — the owner's participation in and conclusion of the brainstorm *is* the approval. Exercised immediately for specs 006 and 007 (both designed question-by-question with the owner in the 2026-07-06 planning session), which move to `status: approved` in the same PR that introduced them as drafts. The no-self-approval rule (AGENTS.md) is unchanged in substance: an agent still may never approve a spec on its own judgment — what changed is that for co-brainstormed specs the sign-off is given in-session rather than as a later, separate act. The `draft_specs` convention (decision 23) remains in place for any spec an agent drafts without the owner having settled its design in-session.

25. **AI-advisory review moves to an in-chat Claude skill; `ANTHROPIC_API_KEY` will not be provisioned (2026-07-06, supersedes the funding question left open by decision 19)**: the repo owner decided the advisory duplicate/boundary review (spec 004's function) will be performed interactively — a Claude Code skill invoked in the owner's own chat session when reviewing a capability PR — rather than by funding a metered API key for the CI job. Costs are covered by the owner's existing Claude subscription *interactively*, which does not have the CI-shares-personal-quota problem that killed decision 17 (the review runs inside a session the owner is already using, not as unattended CI burn). The CI job stays exactly as decision 19 left it: wired, correct, and dormant, posting the `DEGRADED_COMMENT` notice — spec 004's FR-001–004 govern the CI behavior and are unchanged; the skill is additive session tooling fulfilling the same advisory function through a different channel. Human judgment remains the only gate either way. Skill creation tracked as its own registry ticket.

26. **Resuming #9 (traverse-registry crate extraction) re-gated on two live PRs, split by repo scope, spec re-adoption sequenced behind the crate's physical arrival (2026-07-11)**: the pause recorded against #9 (blocked on Traverse branches `507-integration-tests-thread-pool`, `513-bump-version-script`, `515-v1-gate-doc` as of 2026-07-05) was stale — all three resolved by 2026-07-11 (507 merged as traverse#528; 515 merged as traverse#530; 513's actual work landed via traverse#531/#533, the literal branch was an orphaned leftover). But two *new* open PRs were found touching `crates/traverse-registry/` right now — traverse#619 (security: artifact trust classification) and traverse#626 (traverse-starter pipeline) — the same category of collision risk the original pause existed to prevent, just with different branches. Decided in a single `/brainstorm` session, one question at a time: (a) wait for #619 and #626 to merge before resuming, rather than proceeding into an active collision, especially given #619 is security-sensitive; (b) this brainstorm session itself satisfies the repo's no-self-resume rule (a standalone, explicit go-ahead) — no further confirmation is needed once #619/#626 merge, since the "should we resume" question is fully settled now, only the timing was open; (c) execution splits by the 2026-07-09 registry-scope-only rule — the registry side does only the spec re-adoption (specs 005/007/011/034–037/039/041/043 into this repo's `approved-specs.json`), the traverse-side crate move/`Cargo.toml` rewiring/test verification is explicitly *not* done by the agent operating this repo; (d) that traverse-side work is flagged by opening a new tracking issue in `traverse-framework/traverse` once #619/#626 merge — none currently exists (issue traverse#521 was only the spec-*drafting* work, already closed) — rather than just a verbal note that could be lost; (e) per decision 24, the re-adopted specs go straight to `approved` in this repo's registry, no separate Draft/sign-off gate, since their content and the full/partial-move categorization were already brainstormed with the owner on 2026-07-04 (traverse's spec 051) and reaffirmed today — re-homing an already-settled decision isn't new design; (f) however, that re-adoption is itself sequenced to wait until the crate is *physically present* in this repo (i.e. until the new traverse-side tracking issue resolves), not fired immediately when #9 "resumes" — otherwise `approved-specs.json` would `govern` a `crates/traverse-registry/` path that doesn't exist here yet, the exact kind of declared-vs-real drift spec 001's SC-004 exists to prevent; (g) detection of both waits (the #619/#626 merge, and later the tracking issue's resolution) stays passive — checked next time `/registry-ops` runs, reusing the exact mechanism already governing #26/#29, rather than standing up a new background-monitor process for a two-PR wait of unknown duration; (h) #9 stays one ticket through both sequential blocked states rather than being split into two, since it's one piece of work with two prerequisites and splitting would duplicate the spec-051 context across issues for a two-stage shape only just discovered today. **Consequence**: #9's body and labels were updated to name #619/#626 as the live blocker (replacing the stale 2026-07-05 branch list) and the `needs-spec` label was corrected to `blocked` — spec 051 has been approved since 2026-07-04, so "needs-spec" was never accurate for what #9 was actually waiting on.

27. **`index.json` gains contract provenance fields (`contract_digest`, `contract_url`), agent-drafted as `specs/009-contract-metadata-in-index` (Draft), pending owner sign-off (2026-07-14)**: registry ticket #44, tracing to the already-approved `traverse-framework/traverse` spec `063-registry-contract-materialization` FR-001/FR-002, asked for every public index record to carry a resolvable, digest-verified contract reference alongside the existing artifact reference — closing the gap where Traverse's `registry_ref` consumer path (Traverse #551) could otherwise register a capability from a verified WASM artifact without ever fetching or checking its behavioral contract. Implemented in `scripts/ci/build_index.py`: `contract_digest` is `sha256:<hex>` of the exact `contract.json` bytes read at build time (the same immutable file the index is built from, so no separate verification fetch is needed at build time — the "fetch" and the "verify" share one read); `contract_url` is a `raw.githubusercontent.com` link pinned to the current build's `source_commit`, valid because `capabilities/<namespace>/<id>/<version>/contract.json` paths are structurally immutable (decision 10) — any commit on `main` at or after a file's introduction serves byte-identical content, so pinning to the *current* build's commit (rather than resolving each file's original introducing commit via `git log`) is sufficient and cheaper. Also fixed a latent bug while touching this code: `build_index.py` previously silently `continue`d past an unreadable/corrupt `contract.json`, silently shrinking the public index — it now hard-fails the build with a stable `index.contract_unreadable` error instead, since a silently-dropped record is the same failure class (an unverifiable record reaching a consumer) this ticket exists to close. Per this repo's no-self-approval-of-specs rule, `specs/009-contract-metadata-in-index` stays `Draft` in `draft_specs` — the code changes are implemented and tested against it (precedent: decision 22 implemented spec 007 while it was still Draft), but the spec's `Approved`/immutable status requires the repo owner's own explicit, standalone sign-off.

28. **#26 closed as superseded by #30; #39's required-checks scope pinned to spec-alignment + capability-validation (2026-07-15, `/brainstorm`)**: two small open decisions resolved one question at a time. (a) **#26** (`Publish doc-approval.extract 1.0.0`) — its real blocker, `traverse#538`, closed 2026-07-14 without ever landing an `extract` contract; its last comment (2026-07-13) had flagged that a distinct `doc-approval.extract` capability would duplicate the already-published `doc-approval.analyze@1.0.0` public surface (same input fields, incompatible nested output schema) and asked the owner to choose between closing as superseded or approving a versioned migration plan. Rather than guess which the silent closure meant, this was brought back to the owner directly: **closed as superseded by #30** (the already-published `doc-approval.analyze`) — no separate `extract` capability will be built under this repo's existing content. No namespace-reservation follow-up needed: no `extract` contract was ever merged, so immutability never attached to anything for this ID. (b) **#39** (spec-alignment not a required status check) — the only real open question was scope: which CI jobs should become required. Decided **`spec-alignment` and `capability-validation` only** — both are deterministic, always-run gates that spec 001 SC-002 wants every merge traceable to; `ai-advisory-review` was explicitly excluded, since it's dormant-by-design (decisions 19/25, no `ANTHROPIC_API_KEY` provisioned) and making it required would either block every PR forever or require treating its `DEGRADED_COMMENT` output as a pass — both contradict the "advisory, not blocking" decision already made. **Execution note**: branch-protection/required-status-check changes are a security-settings change, which is outside what this agent is permitted to do regardless of approval — #39 stays open, with the exact required-checks list now confirmed, until the repo owner makes the change themselves in GitHub's branch protection UI. **Resolved same day**: the repo owner made the change directly (`spec-alignment` and `capability-validation` added to `main`'s required status checks, confirmed via `branches/main/protection/required_status_checks`); #39 closed.

29. **Crate publish pipeline split out of #9 as its own prerequisite (`specs/010-crate-publish-pipeline`, approved), a placeholder `traverse-registry` 0.0.1 reserves the crates.io name immediately, fresh-start history for the eventual move, `traverse`-side cutover is one atomic PR pinning an exact version (2026-07-18, `/brainstorm` with the repo owner, cross-repo with `traverse#627`)**: resuming #9 (decision 26) surfaced that this repo has zero Cargo/Rust structure today, and that the two tracking tickets' DoD both assumed "depend on the externally-published crate" without ever ticketing the work of making that crate publishable. Six questions, decided one at a time: (a) **history** — the eventual crate-content move imports no git history into this repo; a fresh-start snapshot is taken instead, since `traverse`'s own git log already preserves the pre-extraction history permanently (path-filtered `git log -- crates/traverse-registry`), and grafting an unrelated-histories merge into this repo's own clean, curated commit graph would cost real clarity for no discoverability this repo actually needs. (b) **ticketing** — the scaffold + pipeline work is a new prerequisite ticket in this repo (not folded into #9), so a broken publish pipeline and a broken content move are never the same failure to debug; #9 stays blocked on this ticket resolving first. (c) **spec coverage** — a new spec (`010-crate-publish-pipeline`) governs the Cargo/CI surface specifically (`Cargo.toml`, `crates/traverse-registry/`, `.github/workflows/publish-crate.yml`, `scripts/ci/publish_crate.sh`), kept separate from the specs being re-adopted for #9 (005/007/011/034–037/039/041/043), which govern registry/capability semantics inherited from `traverse` — a different concern that was never meant to cover build tooling. Per decision 24 (co-brainstormed specs are approved by the brainstorm itself), spec 010 goes straight to `approved` in the same PR that introduces it. (d) **name reservation** — the prerequisite ticket's placeholder crate publishes under the exact final name, `traverse-registry`, at `0.0.1`, immediately once the pipeline works — not a throwaway test name reserved later — because this org already lost the identical race once (`traverse-cli` → `traverse-cli-rs`, `traverse` PR #743, days before this session) and there is no reason to reopen that exact risk window. (e) **`traverse`-side cutover sequencing** — once the real crate publishes at `0.8.0`, `traverse`'s Cargo.toml swap, `crates/traverse-registry/` deletion, and `approved-specs.json` trim happen in one atomic PR there, not staged — the correctness check (does the moved code still work) already happens in this repo's own CI before that PR is even opened, so a staged rollout on the `traverse` side would add feature-flagging complexity to catch a failure mode nothing new. (f) **version pinning** — `traverse` pins the dependency with an exact version (`=0.8.0`), bumped deliberately via a reviewed PR each time, matching this project's existing lockstep-version discipline rather than taking a caret range's silent, unreviewed updates — this is the first genuinely external, independently-versioned dependency `traverse` will have, and every other governance mechanism in this org is deliberate rather than automatic. **Execution boundary, consistent with decision 26**: this brainstorm authored the new registry-side ticket and spec 010, and updated both #9 and `traverse#627`'s bodies to reflect all six decisions — it did not implement the scaffold, the CI workflow, or the placeholder publish itself; that is separate, explicitly deferred work. **Also consistent with decision 28**: granting this repo access to the org's existing `CARGO_REGISTRY_TOKEN` secret is a security-settings change the repo owner makes directly in GitHub's org settings, not something the pipeline's implementation can do or wait on — spec 010 FR-004 requires the pipeline fail cleanly and legibly until that grant happens, the same shape decision 19 already established for a different dormant-secret dependency.

30. **Registry-side content governance for the extracted crate resolved as a hybrid: 2 specs re-adopted verbatim, 19 covered by one blanket inherited-governance spec (2026-07-18, `/brainstorm` continuation of entry 29)**: entry 29 covered the publish-pipeline prerequisite; a follow-up question surfaced while double-checking ticket completeness -- of the (now confirmed) 21 `traverse` specs whose `governs` includes `crates/traverse-registry/`, only `005-capability-registry` and `011-event-registry` are actually registry-exclusive there (their only other listed path, `crates/cogolo-registry/`, is dead pre-rename cruft that doesn't exist on disk); the other 19 also govern `traverse-runtime`, `traverse-cli`, or `traverse-contracts`, none of which are moving. Three options were weighed: full re-adoption of all 21 (maximum traceability, but ~19 specs' worth of real synthesis work extracting registry-relevant FRs from requirements that often describe a registry/runtime or registry/CLI interaction as one inseparable requirement, plus permanent dual-copy drift risk), a single fully blanket spec covering all 21 including the two clean ones (simplest, but throws away free precision on 005/011 for no reason), or a hybrid. **Decided: hybrid.** `011-capability-registry-adoption` and `012-event-registry-adoption` are verbatim copies of `traverse`'s 005 and 011 (full FR-level content preserved, zero synthesis needed since they were already registry-exclusive) -- `013-inherited-registry-governance` is a new, deliberately non-restating spec that names the other 19 source specs, states the extracted crate's behavior is governed *as-is* from its state at extraction, and requires (FR-002) that any future behavior change go through a new registry-side spec rather than an edit to the inherited list. **Enforcement timing changed mid-session**: all three were initially drafted to sit unregistered (approved in content per decision 24, but withheld from `specs/governance/approved-specs.json`) per entry 29's decision 26(f)-consistent caution against declaring governance over a path that didn't exist yet. Before this PR was opened, the repo owner directly authored and merged #52 (implementing #50: the Cargo scaffold, placeholder `traverse-registry` 0.0.1 package, and publish-CI pipeline from spec 010) -- `crates/traverse-registry/` now physically exists, so all three specs register immediately instead, per the same reasoning that already governs spec 010 registering against a path whose content doesn't yet fully satisfy it (precedent: decision log entry 22, spec 007 implemented while still Draft -- spec-then-implementation is the normal order, not a claim of already-satisfied FRs). `traverse#627` and `traverse` decision log entry 29's snapshot are unaffected by this entry -- the 19-spec source-list correction (10 originally named in #9's text, 21 actually current) belongs to entry 29's scope, already recorded there and in `traverse#627`'s DoD.

31. **Discovery, not a decision: `traverse-registry` 0.8.1 already published from `traverse`'s own repo before this repo's spec-010 placeholder, invalidating the `0.8.0` real-publish target; resolution deferred to the repo owner via #54 (2026-07-18)**: closing out #50 (decision 29's publish-pipeline prerequisite), the `v0.0.1` tag push succeeded and `traverse-registry` 0.0.1 published to crates.io as planned. Checking the full crates.io version history immediately after turned up `0.8.1`, published **8 hours earlier** (2026-07-18T07:38 UTC vs. this repo's 15:23 UTC), from a `push v0.8.1` tag event in `traverse-framework/traverse` itself — real, functional code straight from `traverse`'s own `crates/traverse-registry/` (dependencies on `traverse-contracts`, `semver`, `serde`, `sha2`), confirmed via that repo's Actions history and entirely unrelated to this repo's spec-010 work. Root cause: `traverse-registry` has always been one of the seven crates `traverse`'s own `publish_crates.sh` ships on every release — its publish had simply never succeeded before because `traverse` itself lacked `CARGO_REGISTRY_TOKEN`, until `traverse#741` granted it that same morning. **Why this isn't just a name-squatting non-event**: the name itself was never at risk of leaving the org (same publishing account both times), but spec 010's plan (decision 29d/e/f) assumed a placeholder-then-`0.8.0` sequencing where the eventual registry-governed publish becomes the crate's *resolved default* — that assumption is now broken, because `0.8.0 < 0.8.1`, so a future `0.8.0` publish here would never outrank the `traverse`-sourced `0.8.1` for a plain `cargo add traverse-registry`. It is also not a one-time collision: `crates/traverse-registry/` still lives in `traverse` and will keep being published there on every subsequent `traverse` release, now that its token works, for as long as #9's cutover hasn't landed — the risk window spec 010 tried to close in one move is actually open-ended. **Decision**: this entry records the discovery only; it does not resolve it. Per this repo's cross-repo-and-spec-immutability guardrails (decisions 26/28's precedent — an agent scopes and surfaces a cross-repo/security-adjacent problem but doesn't execute the fix), no crates.io action was taken (`0.8.1` was left un-yanked), spec 010's approved/immutable FR-006 was not edited in place, and a new tracking issue (**#54**) was opened instead to carry the actual version-strategy decision, which needs the repo owner's explicit call and likely `traverse`-side coordination. #9's Definition of Done (`=0.8.0` / `0.8.0` targets) and `traverse#627`'s matching DoD line are both now known-stale pending #54.

32. **#54 resolved: `traverse-registry` goes independently versioned; real-publish version computed at cutover time, not hardcoded; `traverse#627`'s atomic-removal staging (decision 29e) reaffirmed unchanged (2026-07-18, `/brainstorm` continuation of entry 31)**: entry 31 recorded the `0.8.1` collision discovery without resolving it. Five questions, decided one at a time, with one self-correction mid-session. (a) **stop the ongoing bleed early?** — first leaned yes (ask whoever owns `traverse`'s release process to drop `traverse-registry` from `publish_crates.sh` immediately, freezing the floor at `0.8.1`), but reading `traverse#627`'s existing body (already independently carrying this exact discovery, apparently found the same day on that side) showed the removal is already a DoD item there — scoped to land atomically with the full crate-move cutover PR, per decision 29e's explicit "not staged" call. **Corrected to: no standalone early drop.** Reopening a deliberate prior staging decision needs a stronger reason than a second session preferring it sooner; the bleed is real but bounded (#9 is still upstream-blocked, cutover isn't imminent), and decision 29e's reasoning (avoid feature-flagging complexity for a failure mode nothing new) still holds. (b) **future versioning scheme** — independent, not lockstep with `traverse`'s workspace version: this repo now owns the crate's releases and its own PR/CI gate, and tying every future registry-side patch to `traverse`'s own version bumps is an awkward fit for a separately-governed repo. (c) **target version for the real extraction publish** — first landed on a hardcoded `0.9.0` (next-minor-above-the-then-current-floor, assuming (a)'s early freeze), but (a)'s reversal reopened this: without an early freeze, `traverse` can keep shipping `traverse-registry` releases right up to cutover, so any number picked today could itself be stale by execution time, the same failure mode that just invalidated `0.8.0`. **Corrected to: computed at cutover execution time** — whoever executes #9 checks `traverse-registry`'s actual highest published version at that moment and targets the next minor above it, not a number decided in advance. `traverse`'s own consumption keeps pinning exact (decision 29f unchanged), just to whatever that computed number turns out to be instead of a stale literal. (d) **this repo's `0.0.1` placeholder** — left un-yanked; it's harmless (nothing depends on it, cargo already resolves past it), and yanking is a visible, semi-irreversible action for no practical benefit over the honest version-history record. (e) **spec 010's stale `FR-006`** — no new/amending spec drafted. Its substantive requirement (same workflow/trigger/gate sequence, zero pipeline changes at content-move time) still holds; only the illustrative `0.8.0` figure was wrong, and that figure isn't itself CI-gated (spec-alignment checks path coverage, not FR literal text) — this decision-log entry plus the DoD text in #9/`traverse#627` carry the correction, consistent with this repo's "spec wins over the log on substance, not on illustrative numbers" reading. **Consequence**: no new `traverse`-side ticket was opened (superseded by (a)'s reversal — `traverse#627` already tracks the real fix correctly); that issue's body was instead corrected in place: its "cosmetic only, no action needed" framing understated the risk (true only for `traverse`'s own exact-pinned consumption, not for a plain `cargo add traverse-registry` from any other consumer, which would silently get the un-governed `traverse`-sourced release) and its `=0.8.0` DoD lines were replaced with the compute-at-cutover instruction. #9's DoD was updated the same way. #54 is closed as resolved by this entry.

33. **`specs/014-extraction-compatibility` approved: minimal legacy-ID passthrough entries, not Codex's alias/retirement-lifecycle design; two pre-existing `traverse`-side stale spec-ID bugs found and left unfixed; `013`'s source-specs table amended for a 20th spec it missed (2026-07-21, `/brainstorm` with the repo owner)**: `#9` and two new sub-tickets (`#58`, `#59`, from a `Codex` session) had gone `In Progress` with no branch or PR behind them and no decision-log record for the design choices `#58`'s body claimed were "approved in this conversation" -- unlike every other co-brainstormed decision in this repo's history, which always gets a log entry the same session. Rather than take that claim at face value (this repo's no-self-approval rule exists precisely so an agent's own assertion of prior approval isn't sufficient), the underlying problem was re-investigated from scratch with the repo owner. **The real mechanism**: the crate resolves its own governing-spec registry at compile time, relative to its own directory (`concat!(env!("CARGO_MANIFEST_DIR"), "/../../specs/governance/approved-specs.json")`) -- `approved_spec_registry_contains()` checks embedded `governing_spec` strings against that file's entries, `is_governed_artifact_path()` checks path prefixes the same way. Physically relocating the crate changes which file that resolves to, silently breaking both checks for unchanged pre-existing behavior. **Full audit of the crate's compiled source** (all files, not just the ones `#58` guessed at) found every literal spec-ID string it embeds -- 13 total, cross-checked one at a time against `traverse`'s own current `approved-specs.json`: 10 are real and currently approved there (`002-capability-contracts`, `005-capability-registry`, `007-workflow-registry-traversal`, `011-event-registry`, `037-semver-range-resolution`, `039-connector-plugin-architecture`, `043-module-dependency-management`, `044-application-bundle-manifest`, `046-public-cli-app-registration`, `055-registry-sync`); `999-unapproved-spec` is a deliberate negative-test fixture that must never be registered anywhere; and **`015-metadata-graph` and `026-federation-registry-routing` don't exist in `traverse`'s current governance file at all** (015 is actually `015-capability-discovery-mcp` there today, 026 is `026-event-broker`) -- meaning those two constants are already broken in `traverse`, today, before any extraction, a latent `traverse`-side bug unrelated to this work. **Decided**: (a) replace Codex's proposed alias-manifest-plus-retirement-window design with a minimal passthrough -- one `approved-specs.json` entry per verified-real legacy ID, `status: approved`/`immutable: true`/`governs: ["crates/traverse-registry/"]`, pointing at a one-paragraph stub file under `specs/014-extraction-compatibility/legacy-ids/` (a unique real file per entry, since the spec-alignment gate rejects duplicate `path` values across entries) -- zero Rust source changes, zero new lifecycle machinery, since the goal is preserving already-existing behavior, not migrating it; (b) the two dead IDs are deliberately left unregistered here -- adding entries for IDs that were never valid would change behavior (incorrectly-accepted instead of correctly-rejected), not preserve it, and fixing a `traverse`-side bug is out of this repo's scope; (c) `013-inherited-registry-governance`'s source-specs table gets `002-capability-contracts` added (a 20th spec its original 19-spec audit missed), licensed by that spec's own FR-003 re-verification requirement, not an unlicensed edit to an approved spec; (d) per decision 24 (co-brainstormed specs are approved by the brainstorm itself), `014-extraction-compatibility` goes straight to `approved` in the same PR that introduces it, since this session's brainstorm with the owner is the actual approval event -- unlike `#58`'s unverifiable claim, this one has this log entry as its record. **Ticket cleanup**: `#58`/`#59` (Codex's original tickets, no dev thread ever started) reclaimed under `agent:claude` and closed/rewritten against this corrected, much smaller scope, folded into a single PR alongside the spec itself; `#60` had its "compatibility inventory version" DoD line removed (no inventory exists in this design); `#9` reclaimed from `agent:codex` to `agent:claude`. The `015`/`026` `traverse`-side bug is flagged to the repo owner directly rather than filed as a `traverse`-side issue by this agent (registry-scope-only).

34. **#69's decision-needed gaps resolved via `/brainstorm` (2026-07-28)**: `#69` (the umbrella gap inventory) explicitly said not to claim it as one ticket; its safe/workable items (stale docs, cross-repo ownership matrix, spec-status matrix, lifecycle field mapping) were shipped directly as `#73`–`#76`. Six remaining items needed real decisions. (a) **Stub-artifact replacement (section 1.1, P0)** — before asking, verified that real, distinct WASM artifacts already exist in `traverse`'s `examples/` tree for all six capabilities (e.g. `examples/doc-approval/analyze-agent/artifacts/analyze-agent.wasm`, 663 bytes, backed by real `agent.rs` source, a genuinely different digest from the published 36-byte stub) — this wasn't assumed, it was checked directly before framing the question. **Decided**: republish using these existing `traverse`-side builds (not commission new production-grade agents — that's open-ended work with no owner; not blocked on deciding that first, since the `examples/` builds are real, distinct, already-built content that closes the P0 gap now). **Version**: `1.0.1` (patch — the contract/schema isn't changing, only the artifact behind it, going from placeholder to real), with the stub `1.0.0` yanked immediately on the same PR that publishes `1.0.1`, rather than left to coexist — prevents any consumer from ever resolving the stub via a caret range going forward. (b) **Yank drill (section 4.1)** — resolved as a direct consequence of (a), not a separate decision: yanking the six stub `1.0.0`s the moment their real `1.0.1` successors ship *is* the first production exercise of spec 005's yank mechanism, on genuinely low-risk content (nobody depends on the stubs in production). (c) **Catalog tiers policy (section 1.3)** — deferred: every published capability today is core-team reference content from one publisher; a tier taxonomy (kit/certified/community) designed against one data point risks getting the categories wrong, revisit once a second real publisher or a genuinely production-hardened capability exists to contrast against. (d) **Security advisory policy (section 4.2)** — deferred for the same reason: no third-party publisher yet, no external trust boundary, no real incident to design a disclosure process against. (e) **Events/workflows public-scope (section 5.5)** — capabilities-only for v1: specs 011/012 already cover events/workflows for the extracted crate's *inherited* behavior, but no public, `capabilities/`-style productization path exists or has been asked for; marked explicitly Future rather than left silently unaddressed. (f) **Project 3 board-hygiene automation (section 5.3)** — investigation, not a decision: reading the "Auto-add to project" workflow's actual filter turned out to require either a logged-in browser session or write access neither available in this session (confirmed via GraphQL introspection: `ProjectV2Workflow` exposes only `id`/`name`/`enabled`, no filter/query field, and the only mutation is `deleteProjectV2Workflow` — no narrower fix is possible via API). Handed back to the repo owner to check the filter directly and report back, rather than guessing at a fix or defaulting to deleting the whole automation.

35. **Correction to decision 34(a): `traverse`'s `examples/` artifacts are fixed-output fixtures, not real per-input logic — republish anyway, but disclose it honestly; real logic tracked as its own follow-up (2026-07-28, `/brainstorm` correction)**: decision 34(a) recommended republishing the six stub capabilities from `traverse`'s `examples/` builds, based on verifying those artifacts are real, distinct, source-backed WASM binaries (true) and inferring that made them a fix for the P0 stub-content gap (not fully checked before recommending). Starting the pilot execution on `doc-approval.analyze` surfaced the gap in that verification: `analyze-agent.rs` never reads its input at all — it's a `#[no_std]` WASI binary with a single hardcoded `static OUTPUT` constant, always written to stdout regardless of what document is passed in. The same pattern was confirmed in `traverse-starter.process` and `traverse-starter.summarize` (identical `static OUTPUT`-only structure); all six share the same `build-fixture.sh` scaffolding, a naming detail that was noticed but under-weighted before the original recommendation. **These are fixture/demo artifacts, not real business logic** — republishing them fixes the most visible symptom (all six sharing one identical digest, trivially provable as non-functional with a single glance) but not the substance `#69` section 1.1 actually cares about (a `registry_ref` consumer still gets canned, input-independent output). **Corrected decision**: republish anyway — six genuinely distinct digests and real WASI execution is still a real improvement over one shared placeholder, and it proves the artifact-hosting/yank pipeline end-to-end on real (if simplistic) binaries — but the republished contract descriptions and `capabilities/README.md` must say plainly that these are deterministic fixture outputs that do not read their input, not imply real per-document analysis. Writing genuine minimal deterministic logic (these capabilities are already scoped as no-model/no-network, so real rule-based logic is plausible, not just ML) is tracked as its own, separately-scoped follow-up ticket, not bundled into this republish. **Process note**: this is exactly why the pilot-one-first plan (this session, prior to entry 34) existed — the mistake was caught before all six were touched, not after.

36. **Project 3's recurring stray-item bug (decision 34f) root-caused and fixed by the repo owner (2026-07-28)**: this session repeatedly found `traverse-framework/traverse` issues swept into this repo's Project 3 board (roughly 20+ items cleaned up by hand across the session) — decision 34(f) recorded that diagnosing it further was blocked (no logged-in browser session, and GitHub's API exposes no read/write access to a project workflow's actual filter, only `id`/`name`/`enabled`). The repo owner checked it directly: Project 3's "Auto-add to project" workflow had its repository filter set to `traverse` instead of `registry` — a straightforward misconfiguration, not an org-wide or intentional behavior. Corrected directly in GitHub's UI (the only path available, per decision 34f). Confirmed clean afterward: Project 3's only non-`registry` item is the "Verify published traverse-registry crate through an isolated consumer" draft ticket (no `repository` field at all, since it's a draft, not a stray).

37. **Real bug caught only by verifying actual downloads, not just computed digests: 3 of 7 republished release assets had the wrong filename, 404ing in production (2026-07-28)**: implementing #79's first real-logic capability (`doc-approval.analyze` 1.1.0), the artifact was downloaded to a local scratch path named `analyze-agent-real.wasm` (to avoid clobbering other local copies) and `gh release create` used that local filename as the uploaded asset name by default -- but the contract's `artifact.url` field (matching every other capability's convention) pointed at `analyze-agent.wasm`. Downloading the artifact from its own declared `artifact_url` immediately after publishing caught this as a 404, rather than assuming the earlier local-digest match was sufficient proof of correctness. **Checking whether the same mistake had been made elsewhere found two more**: the `1.0.1` republish batch (decision 35) had downloaded `meeting-notes.process` and `traverse-starter.process`'s artifacts to disambiguating local filenames (`meeting-notes-process-agent.wasm`, `traverse-starter-process-agent.wasm`, since both share the upstream filename `process-agent.wasm` and were downloaded into the same scratch directory) -- both releases had shipped with those same disambiguated names as the actual asset, silently 404ing against their own contracts' declared URLs since the moment they were merged. **All three fixed** by renaming the release assets via the GitHub API to match what each contract already declared (`PATCH /repos/.../releases/assets/{id}`, no republish/new-version needed since the asset content and digest were already correct -- only the asset's file name was wrong). Re-verified all three afterward by downloading the artifact from its exact public `artifact_url` (not a locally cached copy) and confirming the digest matches and, for `doc-approval.analyze`, that the freshly downloaded binary actually executes correctly via `wasmtime`. **Lesson, not just a one-off fix**: computing a digest from a local file proves the *content* is right; it does not prove the *published, resolvable-by-URL* artifact is right. The actual verification that matters is fetching the artifact from its own declared `artifact_url` fresh, the same way a real consumer would -- this is now the standard this repo's agent applies to every future capability publish, not just documented after the fact.

38. **A real ABI-compliance regression found and fixed while scoping a much larger initiative (three new Claude Skills for capability authoring/extraction/workflow-planning); all six reference capabilities now have genuinely input-dependent, genuinely runtime-executable logic (2026-07-28)**: the repo owner asked for an MVP-readiness assessment (registry publish/discover/versioning), then, satisfied with the answer, asked for a much larger follow-on: fill remaining known gaps, then design three new Claude Skills (author/extend a capability via brainstorm intake, extract capabilities from an existing repo, and plan a workflow's needed capabilities against the registry) intended to live in `traverse-framework/claude-skills`. Scoping that work first required reading `claude-skills`' existing `traverse-app-repo-builder` skill in full (to avoid duplicating it, per the requester's own stated design principle) — the repo owner confirmed the 3 new skills should be built by splitting that skill's proven content, not written from scratch or left to duplicate it. That same research surfaced a genuine, previously-unverified defect: `doc-approval.analyze` 1.1.0 (the prior session's first real-logic capability, decision 35's follow-up) was built with `std` + `serde_json` targeting `wasm32-wasip1` — inspecting its compiled import table (`wasm2wat`/`strings`) showed it imports `wasi_snapshot_preview1::environ_get`/`environ_sizes_get`, which Traverse's `WasmExecutor` ABI whitelist (`host_abi_v1.json`, documented in `claude-skills`' own troubleshooting reference) rejects outright. It had only ever been verified by running it under a generic `wasmtime` host (decision 37), which is far more permissive than Traverse's own runtime — so it could never actually have executed through `traverse-cli agent execute`/`serve`/an embedder SDK, the exact product this registry exists to serve. By contrast, the five fixture capabilities from decision 35 were confirmed ABI-compliant (their only import is the whitelisted `fd_write`), just not input-dependent. Neither state — real-but-unexecutable, or executable-but-fake — was acceptable on its own. **Fix**: built a shared `no_std` + `alloc` runtime shim, `agents/wasi-agent-runtime/` (a bump allocator, a hand-rolled JSON parser/writer since `serde_json` needs `std`, and the `fd_read`/`fd_write`/`proc_exit` WASI glue), reused by all six capability crates so each one only has to implement its own pure input→output logic. Confirmed via `cargo build --target wasm32-unknown-unknown --release` (not even a manual `rustc` invocation was needed) that every resulting binary imports exactly the three whitelisted WASI functions, and via `wasmtime run` that each genuinely computes distinct, correct output for distinct input (unit tests plus hands-on execution for every capability). All six capabilities were republished: `doc-approval.analyze` 1.1.0 is now itself deprecated for the ABI defect, superseded by 1.2.0 (not 1.1.1, since 1.1.0 was never withdrawn before the fix); the other five move `1.0.1` (fixture) → `1.1.0` (real). Every prior version stays published and resolvable by exact pin, per the additive-yank rule (spec 005) — nothing was edited or deleted. Artifact-filename verification followed decision 37's standard (download from each version's real public `artifact_url`, confirm digest, and for two capabilities, execute the freshly downloaded binary directly) rather than trusting local build digests alone. The three-skills work itself (and a second phase parameterizing `traverse-cli`'s hardcoded registry source repo for npm-style private registries) is scoped as separate, later checkpoints per the repo owner's explicit instruction to pause between phases before any PR opens in `traverse` or `claude-skills`.

39. **Phases B and C shipped (npm-style private-registry support in `traverse-cli`; three Claude Skills replacing `traverse-app-builder`); dogfooding those skills against five real, published capabilities surfaced and fixed a production incident (2026-07-28/29)**: continuing directly from decision 38's checkpoint. **Phase B** (`traverse-framework/traverse#843`, merged): `registry sync` gained `--source-repo <owner/repo>`, `capability publish` gained `--registry-repo-remote <owner/repo>`, and `TRAVERSE_REGISTRY_TOKEN` env-var auth for private sources — any GitHub repo (a team's own fork/clone of this repo's layout) is now a valid sync/publish target, resolving the "team-shared private registries" item this log had listed as deferred since decision 20. **Phase C** (`traverse-framework/claude-skills#6`, merged): `traverse-app-builder` retired, its verified content split into `traverse-capability-author`, `traverse-capability-extractor`, and `traverse-workflow-planner` per the repo owner's explicit design principles (avoid duplication, atomic, generic, explicit public/private and secrets decisions). Getting `claude-skills`' own PRs mergeable first required fixing that repo's incomplete org-governance onboarding (`claude-skills#2`, pre-existing, unblocked via a temporary ruleset bypass the repo owner granted and then revoked).

The repo owner then asked to use `traverse-capability-author` for real — research five candidate utility-tier capabilities (`validation.validate-email`, `validation.normalize-phone-number`, `validation.score-password-strength`, `validation.validate-luhn`, `formatting.format-currency`), review the drafted contracts, then publish, test, and fix problems found along the way, one capability at a time. This is the first time `capability publish` was used for a real publish in this registry's history (the original six reference capabilities were hand-committed directly, decision 34 onward) — it surfaced three real, previously-latent defects:

- **`wasi-agent-runtime`'s JSON parser could never parse `Value::Bool(false)`**, in any position — `parse_bool` tried `consume_literal("true")` unconditionally first, and `consume_literal` has no backtracking, so a failed match against `false` input still consumed the `'f'`, corrupting the stream before the `"false"` attempt even started. Latent since decision 38 (none of the six original capabilities have a boolean *input* field); surfaced immediately once one did. Fixed by peeking the first character before committing to one literal branch; the fix itself was built and used locally for a full working session before actually being committed to this repo (`#94`) — a real process lapse, caught only when a later, unrelated PR (`#94` itself) hit a merge conflict against `main`'s still-broken copy.
- **`traverse-cli capability publish` silently drops any contract field `CapabilityContract` (in `traverse-contracts`) doesn't model** — confirmed by reading the struct directly: no `artifact` or `artifact_type` fields exist there at all, and `capability_publish_plan` writes the registry file by re-serializing that struct, not the author's original JSON. This produced a real incident: `validation.validate-email` and `validation.normalize-phone-number` published with `artifact` correctly supplied, came out the other end without it, and `build_index.py` copied the resulting `None` straight into `index.json` as `digest: null`/`artifact_url: null` — which crashed `registry sync`'s deserialization for *every* consumer, not just those two capabilities, the moment either record loaded. Filed upstream as `traverse-framework/traverse#859` (not yet fixed there). Fixing it required two registry-side PRs, not one: deprecating and republishing the two broken versions (`#92`) turned out *not* to actually resolve `registry sync`, because deprecation only flags a record, it doesn't exclude it from the index, and the broken 1.0.0 contracts can never be edited (immutability) — `registry sync` was re-verified broken even after `#92` merged. The real fix (`#93`) changed `build_index.py` itself: a deprecated record missing `artifact` info is now excluded from the index entirely (the only way a future build can succeed, since the underlying file is permanently unfixable), while an *active* contract in the same state now hard-fails the build, extending decision 27's "an unusable record must never reach a consumer" precedent to this failure class. Verified resolved via a completely fresh `registry sync` after `#93` merged and the index rebuilt — not assumed from the PR merging cleanly.
- **`capability publish`'s auto-generated PR body cites `056-capability-publish`/`054-public-scope-registry-ref` — both `traverse`-repo spec IDs — regardless of which registry it's publishing to**, so every PR it opens against *this* registry fails `spec-alignment` on the first run, unconditionally. Filed as `traverse-framework/traverse#858` (not yet fixed). Worked around on every publish PR this session by manually editing the body to the correct registry-repo specs and force-triggering a fresh CI run — editing a PR body alone doesn't retrigger `pull_request`-scoped checks, and even `gh run rerun` replays the original, stale cached webhook payload; only an actual new commit (`synchronize` event) causes GitHub to re-evaluate against the current body. This is now the established recipe for every future publish PR until `#858` is fixed upstream.

All five capabilities are published and healthy (`validation.validate-email`/`normalize-phone-number` at 1.1.0 after the incident; `score-password-strength`, `validate-luhn`, `formatting.format-currency` at 1.0.0, built with the parser fix and the artifact-field workaround applied from the start). Two of the five also had genuine spec-vs-implementation gaps caught only by implementing and testing, not by review: `normalize-phone-number` didn't handle domestic trunk-prefix numbers (e.g. UK `020 7946 0958`) and now discloses that plainly rather than silently; `score-password-strength`'s original spec prose didn't reconcile with its own worked examples, corrected to match a verified, self-consistent scoring model. Every published contract's `provenance.spec_ref` is deliberately `null` — `008-reference-capability-publication` was checked and ruled out (scoped to the `traverse-starter`/`doc-approval`/`meeting-notes` seed set specifically), and no other approved spec governs a general-purpose utility capability like these today; flagged to the repo owner as a real, open capability-boundary question (utility function vs. "one meaningful business action," per the `capability-review` skill's own rubric) rather than resolved unilaterally.

40. **Discovery gap escalated into a real build plan: GitHub Pages catalog + `traverse-mcp` search tool, `use_cases` promoted into the contract schema, all decided via `/brainstorm` (2026-07-29)**: two follow-up questions surfaced a pair of previously-unfiled gaps — asked "where/how will people find capabilities as the registry grows to hundreds or thousands, and did we file tickets for every gap found this session" — verified directly (fresh `traverse-cli --help`, tracing commit `7ca6a08`) that today's only discovery mechanism is `registry list`/`search`, a substring match on `namespace`/`id` only, with no search over what a capability actually does. Filed **#100** (namespace-organization policy — my own 5 utility capabilities used topic-based namespaces, an undocumented departure from the app-scoped convention spec 008 established for the original 6), **#101** (utility-tier vs. business-capability boundary — discussed at length in PR bodies and the `capability-review` skill's output, but never filed as its own tracked issue until now), and, after confirming `traverse#814` (browse/search) shipped only the substring MVP and its own "resolve summaries for real browsing" suggestion was never split into a follow-up, **#102** (no discovery over what a capability does). A related, already-deferred item (**#69** section 1.3 / **#85**, catalog tiers) was checked against its own stated reopen trigger ("a second real publisher or a genuinely production-hardened capability exists to contrast against") — the owner explicitly chose to leave it deferred rather than reopen it now, on the reasoning that #101's utility-tier question is more load-bearing and should resolve first (a tier taxonomy designed before deciding whether utility-tier capabilities belong in this registry at all risks needing to be redone).

The owner then escalated #102 into a verdict: without at least an MCP surface and/or a searchable web page indexed by use case, the registry doesn't deliver on its purpose. Resolved into a concrete build plan via `/brainstorm`, one question at a time:

- **What to build**: both a human-facing catalog and an agent-facing search tool — not a new, separately-hosted MCP server. Checked directly first (cloned `traverse-framework/traverse`, read `crates/traverse-mcp/src/tools/capabilities.rs` and spec `209-capability-discovery-mcp`): a real `traverse-mcp` crate already exists, with `list_capabilities`/`get_capability` tools that already return each capability's `description`, filterable only by `service_type`/`permitted_targets` — no free-text search. It runs as a local stdio process, the normal way MCP servers work (an agent's host launches it locally) — which GitHub Pages, being static-hosting only with no server process, cannot run. **Decided**: GitHub Pages serves the static catalog only; `traverse-mcp` gets a new `search_capabilities` tool, filed as a companion issue (`traverse-framework/traverse#876`) rather than duplicating a server that already exists.
- **Catalog builder dogfoods Traverse**: a plain script (matching `build_index.py`'s own precedent) gathers every `capabilities/**/contract.json` into one JSON bundle — necessary because the WASM ABI whitelist verified earlier this session (`fd_read`/`fd_write`/`proc_exit` only, no directory listing) means a capability cannot walk a file tree itself — then a new Traverse capability (`no_std`/WASM, same shape as every agent under `agents/`) transforms that bundle into the catalog's HTML + a `search-index.json`. Considered going further (the WASM capability also generating HTML templating itself) but decided against it: the no_std/no-libm constraints already hit repeatedly this session (bump allocator, hand-rolled JSON, avoiding `f64::round`) make HTML templating inside a `no_std` crate meaningfully riskier than the JSON-in/JSON-out transform shape every capability so far has used successfully.
- **Search depth / schema**: the owner specifically asked for search "per usecases," not just by name — checked directly that `contract.json` has `summary`/`description` for all 11 published capabilities today, but real worked use-case detail (happy/unhappy paths, examples) only exists in SPEC.md files under `agents/`, written for the 5 newest capabilities only, and not part of the public contract schema at all. **Decided**: add `use_cases: [{scenario, input_example, output_example, happy: bool}]` to the contract schema — a narrower, search-focused promotion of what SPEC.md already contains, not the full NFR/interfaces/configuration set (SPEC.md stays the fuller supplementary doc). **Governance**: amend `specs/001-registry-foundation/spec.md` directly with a new FR, rather than layer a new spec on top — a deliberate, explicit one-off exception to the usual pattern (decisions 22, 29c: new specs layer on spec 001 rather than edit it), the owner's own call this time, reasoning that `use_cases` is core enough to belong in the foundational schema itself.
- **Backfill scope**: all 11 currently-published capabilities, now — not just the 5 newest with SPEC.md content ready to promote. The original 6 reference-app capabilities have never had use-case content written before; this is new authoring work, not just a restructure, and is scoped as its own child issue rather than assumed to be quick.
- **Relation to #99** (a separate, already-open umbrella for workflow/content-group discovery aimed at kit/OS-shell MCP consumers, companion `traverse-framework/Traverse#865`): kept separate but cross-linked, not folded in — different scope (capability-level search vs. workflow catalog), but should share search infrastructure once both exist rather than rebuilding it twice; commented on `#99` accordingly.
- **Issue structure**: filed as an umbrella (**#103**) decomposed into bounded child issues — matching **#69**'s own precedent exactly, for the same reason #69 stated it (avoid an unclaimable mega-ticket): **#104** (spec 001 `use_cases` FR amendment), **#105** (catalog-builder capability + gather script), **#106** (GitHub Actions → Pages deployment on merge to `main`, same trigger as spec 001 FR-006's index build), **#107** (backfill `use_cases` across all 11 capabilities), and the cross-repo companion **`traverse-framework/traverse#876`** (`search_capabilities` MCP tool).

**Execution boundary**: this brainstorm decided the design and filed the tracking issues; it did not implement any of the five children. That is separately scoped, later work, consistent with how prior large initiatives in this log (decisions 29, 38) have been checkpointed between deciding and building.

41. **"Agent" terminology corrected: reserved for AI-model-backed capabilities only; `agents/` renamed to `capability-src/`; cross-repo `traverse-cli` verb renamed outright, no alias (2026-07-29, `/brainstorm`)**: the repo owner pointed to their own published reference work (`enricopiovesan/UMA-code-examples`, chapter 13) as grounding — in that framework, "agent" is reserved specifically for real AI-model-backed reasoning components (`PlannerAI`, `SummarizerAI`, `TranslatorFr`, directoried `*-ai-wasi/`), never for plain deterministic logic, which is just a "capability." This repo's own convention — every capability's WASM source lives under `agents/<name>/` (first introduced in PR #88, decision-log entry 38) — conflicts with that: all 11 capabilities currently published here are pure deterministic business logic (Luhn checksum math, regex-shaped validation, currency formatting), zero AI/LLM involvement, so calling their source directory "agents/" is a genuine mislabel. Resolved via `/brainstorm`, one question at a time:

- **New convention**: "agent" becomes formally reserved, going forward, for a capability whose implementation genuinely involves AI/model-backed reasoning — this registry has zero agents today, only capabilities. The source directory renamed `agents/` → `capability-src/`.
- **Scope: both repos, decided together**. This repo's own directory/spec naming *and* `traverse-framework/traverse`'s live `agent execute`/`agent serve` CLI verbs (which actually run any capability, AI-backed or not — a separate, looser use of "agent" as "the runtime-executed unit," not a signal of AI involvement) both get corrected in the same pass, rather than fixing one and leaving the other inconsistent.
- **CLI verb migration**: `traverse-cli agent execute`/`agent serve` → `capability execute`/`capability serve`, renamed outright with **no alias and no deprecation warning** — the repo owner's explicit call, prioritizing a clean end state over a migration runway for existing scripts/docs/CI referencing the old verb.
- **Spec migration mechanics**: decided to edit affected specs directly rather than layer a new spec on top (this is a pure terminology correction, not new behavior, so there's no new FR to layer) — but **executing this immediately surfaced that the premise was wrong**: a grep for the word "agent" across `specs/` had found 6 files (`001`, `007`, `009`, `010`, `011`, `013`), but reading every actual hit in context showed all six were false positives — "an AI agent" (a coding assistant reading the spec), "Drafted by an agent"/"Not Agent-Executed" (again, an AI coding assistant, not capability execution), "developer or agent" and "human and agent discovery" (registry *consumers*, not implementations), and `035-multi-agent-isolation` (a verbatim, un-owned title of a different Traverse spec). **None of these 6 specs actually define `agents/` as a governed path or `agent execute`/`serve` as a formal requirement** — confirmed by also checking `specs/governance/approved-specs.json` for any `governs` entry naming an `agents/` path (none exists). **No spec edits were needed or made.**
- **Relation to #101** (utility-tier vs. business-capability): decided this is a different, orthogonal axis — business-vs-utility is about the granularity/scope of what a capability does; deterministic-vs-AI-backed is about how it's implemented. Kept #101 scoped to its original question rather than folding the two together.
- **Execution timing**: decided now, not ticketed — unlike the discovery-catalog work (entry 40, a new feature that can wait), this is a live mislabeling judged to get more entrenched the longer it sits.
- **A real complication surfaced mid-execution, before any files were touched**: six already-published, permanently immutable records (`doc-approval.analyze` 1.2.0's `contract.json`, `doc-approval.analyze` 1.1.0's `deprecated.json`, `doc-approval.recommend` 1.1.0, `traverse-starter.process`/`validate`/`summarize` 1.1.0, `meeting-notes.process` 1.1.0) have `agents/...` paths or `traverse-cli agent execute`/`serve` written directly into their `description`/`reason` text — text that, per this repo's immutability rule, can never be edited. Brought back to the owner before proceeding rather than assumed away: **decided to rename anyway and disclose the historical mismatch** (same provenance-over-current-accuracy principle already applied elsewhere in this repo, e.g. spec 007's historical `process-agent.wasm` filename), over the alternatives of republishing all 6 at new versions just to fix prose (real effort for a non-functional change) or not renaming the directory at all (leaves the wrong name on 100% of what's actually published, indefinitely). `capabilities/README.md` now carries an explicit note listing exactly which immutable records predate the rename.

**Executed this session** (registry repo): `agents/` → `capability-src/` (`git mv`, history preserved for all 12 subdirectories); the shared runtime crate `wasi-agent-runtime` → `wasi-capability-runtime`, its `run_agent()` function → `run_capability()`, and every one of the 11 capability crates' `Cargo.toml` dependency path and `main.rs` imports updated to match — verified clean (`grep` for any remaining `wasi_agent_runtime`/`wasi-agent-runtime`/`run_agent(` outside of gitignored `target/` build caches returns nothing) and re-tested (`cargo test` passes for all 12 crates post-rename, including the runtime crate itself). `capabilities/README.md` and `CLAUDE.md` updated to the new paths and terminology; `CLAUDE.md`'s Project Structure section also gained an explicit `capability-src/` entry (it had never documented `agents/` at all, a pre-existing gap noticed while fixing this) and its stale "no build/test commands yet, no executable code" line was corrected — untrue since decision-log entry 38, not just as of today.

**Noticed, not fixed, out of scope for this entry**: `examples/applications/expedition-readiness/components/*/component.manifest.json` reference `../../../../agents/team-readiness-agent/artifacts/validate-team-readiness-agent.wasm` — a path that does not exist anywhere in this repo (`agents/team-readiness-agent/` was never real, pre-dating this session). A pre-existing, unrelated stale reference, not a consequence of today's rename; left untouched rather than scope-creeping this entry.

**Cross-repo**: `traverse-framework/traverse`'s `agent execute`/`agent serve` CLI verb rename is separate work in that repo's own source, tracked and executed as its own PR (see that repo's history for specifics) — this entry records the decision; per this repo's registry-scope-only default (relaxed here only because the owner explicitly authorized both repos together, same precedent as decision 39's Phase B), the actual Rust changes there aren't re-narrated in this registry's own log.

42. **#101 resolved: utility-tier is a legitimate, permanent capability class; no `tier` field, ever — discovery runs on `use_cases`, not a business/utility label (2026-07-29, `/brainstorm`, Step 1 of the decisions-specs-ADRs closeout plan)**: the `capability-review` skill's advisory rubric ("one meaningful business action, not a CRUD wrapper or utility function") had flagged the 5 utility-tier capabilities as a genuine boundary question, disclosed in `capabilities/README.md` but never formally decided. Two questions, decided one at a time:

- **Is utility-tier legitimate?** Yes, permanently — these 5 capabilities are real, tested, useful, already published; unwinding them into a separate shared library would discard real shipped value to satisfy an advisory rubric that itself predates this registry ever having non-reference-app content to test it against. The `capability-review` rubric should be refined to explicitly account for both classes going forward, not discarded, but that refinement is documentation work for whoever next touches that skill, not part of this decision.
- **Does it need schema support?** **No — not deferred, genuinely not needed, as a matter of principle, not data volume.** The owner's reasoning: from the actual consumer's side — an LLM/runtime using MCP to find a capability to complete a workflow step, or a human developer doing the same thing manually — neither needs to know whether a capability is "business" or "utility." Both only need to know whether it solves the atomic problem a given workflow step actually has, and that's exactly what the `use_cases` field (decision-log entry 40, `specs/001-registry-foundation/spec.md` FR-011, not yet written — see #104) is for. A `tier` field would be a classification axis that no real consumer's actual selection logic would ever query on — pure overhead, not "not yet justified by volume" (the reasoning that correctly deferred the separate, different catalog-tiers question in decision 34c/#69 §1.3/#85). This closes off question 3 from the original framing (field naming, backfill scope) as moot — there is no field to name or backfill.
- **Consequence for `capabilities/README.md`**: its existing "Utility-tier capabilities" section stays as historical/session provenance (why these 5 were built, what they're testing) but is not a label any future discovery mechanism (the catalog/search work in #103-#107, `traverse-framework/traverse#876`) should treat as a queryable property. No code or schema change results from this entry.

**Ticket disposition**: #101 closed as resolved by this entry — no follow-on implementation ticket, since the decision is "do nothing to the schema," not "build X."

43. **#100 resolved: namespace policy for general-purpose capabilities is owner/publisher-scoped (`core`), not topic-scoped — applies only where spec 008's app-scoped convention doesn't; the 5 already-published utility capabilities stay exactly as they are (2026-07-29, `/brainstorm`, Step 2 of the decisions-specs-ADRs closeout plan)**: the 5 utility capabilities (`validation.*`, `formatting.*`) used ad hoc topic-based namespaces when published, a different organizing principle than spec 008's app-scoped convention for the original 6, with no general policy governing capabilities that aren't tied to a reference app. Three questions, decided one at a time, with one real pivot mid-brainstorm:

- **Convention**: owner/publisher-scoped, not topic-scoped, not hierarchical. Reasoning: namespace was reserved by spec 001 FR-008 specifically to identify *who* published something ("to avoid a breaking migration when third-party publishing is added later"), not *what topic* it's about — topic-based namespaces would collide the instant a second publisher wants to publish something on the same topic, since namespace can't mean both "topic" and "publisher identity" at once. Discovery-by-topic is `use_cases`' job (a direct consequence of #101's outcome, decision 42), not namespace's — this brainstorm explicitly carried that same "don't build a disguised second classification axis" discipline forward rather than re-litigating it.
- **A real pivot**: the first answer's own recommendation preview said `core` applies "reference-app or not," which — read literally — would mean a *future* reference app should also use `core` instead of app-scoping, silently overriding spec 008's already-approved pattern. Caught and asked as its own question rather than left ambiguous: **decided `core` is scoped narrowly to the residual category only** — general-purpose capabilities with no natural reference app. Spec 008's app-scoped convention is untouched and still the rule whenever a real reference app exists; the two conventions coexist for two genuinely different cases rather than one silently replacing the other.
- **Governance**: becomes a real spec requirement, not documentation-only — folded into the same not-yet-opened PR as #104 (spec 001's `use_cases` FR from decision 40), as a second new FR in the same file (see execution note below for the actual FR number once written). Reasoning: namespace is already a schema-reserved field (FR-008); a policy real enough to have caused a genuine, disclosed inconsistency (the ad hoc topic-scoping) shouldn't stay convention-only indefinitely, the same reasoning that resolved decision 40's `use_cases` field into spec text rather than README prose.
- **The 5 already-published utility capabilities**: left exactly as published, permanently — **not** republished under `core.*`. Reasoning: unlike the `agents/` → `capability-src/` source-directory rename (decision 41, no consumer-facing impact), namespace is part of a capability's public identity; republishing under a new namespace creates a permanent duplicate (immutability means the old `validation.*`/`formatting.*` records stay resolvable forever regardless), actively confusing discovery rather than clarifying it, for zero functional gain. These 5 stay a disclosed historical exception, the same treatment already given to the six immutable contracts whose description text still says `agents/` (decision 41) — `capabilities/README.md` gets an equivalent note once the spec PR lands.

**Execution boundary**: this entry records the decision only. Writing the actual spec 001 FRs (this decision's namespace policy + decision 40's `use_cases` field, both folded into one PR per this entry's own governance call), updating `capabilities/README.md`, and closing #100/#104 are the next, distinct step — not done as part of this brainstorm, matching the pattern decision 42 / #101 just used.

44. **#99 unblocked: 6 real decisions resolved via `/brainstorm`, closing off the twice-deferred catalog-tiers/resolve-policy question along the way (2026-07-29)**: `#99` (LLM MCP kit-catalog umbrella) carried the same "do not claim as a single coding ticket" shape `#69` and `#103` both had before their own brainstorm passes — its own DoD was "create child tickets," not "write code," and several of its six candidate children were explicitly blocked on a decision or spec that didn't exist yet. The owner's own words, prompted by the dev-implementation chat reporting #99 as the one remaining un-actionable item: **"yes I want all the decision to be nailed down other wise the dev cilcle will stop again."** Six questions, decided one at a time, with two real findings mid-brainstorm that reshaped what needed deciding:

- **Reopening the twice-deferred question, confirmed first, not assumed**: `llm-default-resolve-policy` sits in the exact same territory as catalog tiers / security advisory policy (decision 34c/34d, reaffirmed deferred again in entry 40's "I'll decide separately"). Asked directly rather than silently overturned: **yes, resolve it now** — the original deferral reasoning ("one publisher, no real variety yet") no longer fully holds, and #99 shows a concrete, live consequence (an MCP/LLM consumer resolving `^1` could land on a fixed-output fixture instead of real logic).
- **A real finding that reshaped the resolve-policy question**: before deciding a *mechanism*, checked directly whether one already exists — read every version + `deprecated.json` across all 6 kit capabilities. **Every fixture/stub version is already deprecated**; only the real, current versions (`1.2.0`, or `1.3.0` for `doc-approval.analyze`) are live. Spec 005's yank mechanism already excludes deprecated versions from `^1`-range resolution while preserving exact-pin resolvability — meaning the outcome #99 wants (LLM consumers land on real logic, not fixtures) **already happens today**, by hand-followed discipline, not by any written rule. **Decided**: don't invent a new mechanism — formalize the existing, already-correct practice as an explicit spec requirement ("a fixture/stub version MUST be deprecated in the same PR that publishes its real-logic successor"), closing the actual gap (an unwritten norm that could silently lapse on a future publish), not a missing capability.
- **Workflow discovery (`publish-kit-workflows`)**: the public index is capabilities-only today (`{ capabilities: [...] }`); no workflow-index-record concept exists, and `workflows/` has exactly one unpublished example. **Decided**: workflows become first-class index citizens — a new `workflows[]` array, built the same way `capabilities[]` already is, with the same immutable-versioned-directory layout (`workflows/<namespace>/<id>/<version>/workflow.json`).
- **Content groups (`publish-mcp-content-groups`)**: rather than invent a third index record type describing "which capabilities a workflow composes," **decided this is just the workflow record's own membership list** — a workflow.json inherently already names what it composes; a separate content-group record would duplicate that same information in two places that could drift. `meeting-notes.process` (a single-capability entrypoint, no workflow) gets a small `entrypoint`-style marker directly on its own contract instead of forcing a workflow wrapper around one capability. Carries forward the same "don't build a disguised second classification axis" discipline entries 42/43 already established.
- **Tool metadata facet (`mcp-tool-metadata-facet`)**: #99 itself flagged this as lower-priority, but the owner explicitly wanted it decided now rather than left as one more "revisit later" thread. Checked what it was actually asking for against what's already decided: "tool title" is already `summary`; "example requests" is already `use_cases`' `input_example`/`output_example` (entry 40) — both already covered, no new field needed for those. The one genuinely new thing — a list of which output fields are runtime-populated (e.g. a trace id) rather than determined by the capability's own logic, so an MCP prompt-builder doesn't confuse the two — **decided**: add as a small new optional field, `runtime_owned_output_fields: string[]`, defaulting to empty. Zero backfill burden: none of the 11 currently-published capabilities have any runtime-populated output fields today.
- **Kit bundle shape (`mcp-kit-bundle-profile`)**: #99 framed this as a real either/or — a dedicated published bundle artifact, or a documented sync profile over the existing mechanism. **Decided**: documented sync profile ("kit-llm" convention: `registry sync`, then filter the index's `workflows[]`/`capabilities[]` to kit namespaces), not a new artifact type — reuses what `registry sync` + the index arrays (just decided above) already deliver, rather than standing up a second, parallel publish/version lifecycle to keep in sync with the first.
- **Cross-repo scope, asked rather than assumed**: #99 references a Traverse-side companion issue. Consistent with this session's established pattern (decisions 39/41 each crossed into `traverse-framework/traverse` only with their own explicit go-ahead, never assumed from a prior instance) — **decided registry-side only this pass**; the Traverse companion issue is a separate, later authorization if/when it needs updating to reflect these decisions.

**Ticket disposition**: `#99`'s decided children, to be filed as real GitHub issues after this entry: (1) workflow-index + content-group-via-workflow-membership (spec + implementation), (2) example-request artifacts per kit entrypoint (narrowed significantly now that `use_cases` already covers the example data itself), (3) documented `kit-llm` sync-profile convention. The resolve-policy rule and the `runtime_owned_output_fields` field are spec-only additions, not separate implementation tickets — folded into the same spec 001 PR as the workflow/content-group FR, following the by-now-established pattern (decisions 40/43 combined into one PR) of batching same-file spec touches rather than opening near-simultaneous separate PRs against `specs/001-registry-foundation/spec.md`.

**Execution boundary**: this entry records the decisions only, per the same "planning chat" boundary established for the whole decisions-specs-ADRs closeout plan. Writing the actual spec 001 FRs, filing #99's real child tickets, and closing/updating #99 itself are the next, distinct step.

45. **Catalog analytics strategy decided via `/brainstorm`: hosted, privacy-first, no consent banner; page views + search terms (especially empty-result ones); admin-only dashboard; decision-log only, no spec (2026-07-30)**: with the catalog now live at `registry.traverse-framework.com` (custom domain configured this session, DNS + GitHub Pages settings verified working end-to-end), the owner asked for "a strategy for analytics and use of each capability" alongside two other website fixes (dark-theme legibility, #130; sitemap/SEO, #131). Scoped first, before brainstorming: "usage of each capability" could mean website analytics (page views/searches on the static catalog) or real runtime execution telemetry (how often a capability is actually resolved/executed via `traverse-cli`/`registry sync`) — confirmed with the owner these are genuinely different systems (one is this static site, the other means instrumenting `traverse-cli` itself, cross-repo, real privacy tradeoffs for anyone syncing/executing capabilities) and decided **both matter, but as separate decisions** — this entry resolves website analytics only. Five questions, decided one at a time:

- **Provider class**: a privacy-first, cookie-less tool (no consent banner needed under GDPR — no cookies, no PII) — not Google Analytics (cookie-based, needs a consent-banner UI to build just to satisfy legal requirements) and not GitHub Pages' own built-in traffic insights (repo-admin-only, can't capture in-page custom events like search terms, only pageviews/referrers).
- **Hosted vs. self-hosted**: hosted (e.g. Plausible), not self-hosted (Umami/GoatCounter) — this registry has stayed deliberately serverless from its very first design decision (decision 3: "free to run... no server, no database, no bill regardless of usage"); self-hosting analytics would be the first time this repo stood up and maintained a server for anything, a bigger operational shift than it sounds for what's meant to be a lightweight add-on. The small recurring cost of a hosted tool is the actual, deliberate tradeoff being made to preserve that zero-infrastructure posture.
- **What to capture**: page views per capability **and** search-query terms, with empty-result searches specifically surfaced — page views alone only show which already-published capabilities get looked at; a failed search (e.g. "stripe payment", zero results) is a direct, concrete signal of unmet demand, exactly the kind of information that should inform what to build next.
- **Dashboard visibility**: admin-only (the analytics provider's own dashboard), not a new public `/stats` page on the catalog itself — simpler to ship now, and most hosted privacy-first tools support making a dashboard publicly shareable later via a settings toggle with zero migration cost if that's ever wanted.
- **Governance**: decision-log entry only, no spec — unlike FR-011 through FR-015, this doesn't govern the registry's data model, contract schema, or resolution behavior; it's a tooling/instrumentation choice with nothing for CI to check or another spec to reference.

**Runtime usage telemetry, deliberately scoped out, tracked so it doesn't evaporate**: unlike the website-analytics questions above, this has zero decided scope yet (what to measure, where it lives — `traverse-registry`/`traverse-cli` — and real privacy tradeoffs for anyone syncing/executing capabilities are all still open). Rather than leave it as a prose-only note the way the `capability-review` rubric refresh initially was (caught only in a later re-audit, decision-log context around entry 42's ticket #114), filed as its own tracking issue now, explicitly stating it needs a dedicated `/brainstorm` before any work starts.

**Execution boundary**: this entry records the decision only, consistent with this whole session's "planning chat" boundary. Implementing the actual analytics integration (script tag, custom events) is separate work, ticketed as a sibling to #130/#131.

46. **`use_cases[].scenario` must be a full user story, not a plain declarative sentence; FR-011 amended, all 11 existing capabilities to be republished, `traverse-capability-author` template to be updated too (2026-07-30, `/brainstorm`)**: the owner directly identified a real gap — every published capability's `scenario` text (e.g. `validation.validate-email` 1.2.0: "A structurally well-formed address is accepted.") is a plain declarative sentence, not the "As a `<persona>`, I want to be able to..." user-story format they consider "super important." Four questions, decided one at a time:

- **Exact format**: the full, classic 3-part user story — `"As a <persona>, I want to <action>, so that <benefit>."` — not just the 2-part role+want the owner's own phrasing literally quoted. Reasoning: the "so that" clause is what actually makes a scenario useful for discovery (matching a real workflow need, per decision 42's "discovery runs on `use_cases`, not a label"), not just a relabeled version of the existing plain-declarative text with a persona bolted on front.
- **Governance**: formal spec text, not just a documented convention — FR-011 amended directly (in the same repo, same file it already governs) to require this format explicitly, since leaving it as tribal convention is exactly how the current plain-declarative style happened unchecked in the first place.
- **Skill-template scope, decided together rather than left registry-side-only**: `traverse-framework/claude-skills`' `traverse-capability-author` skill (the tool that actually authors `use_cases` for every new capability) needs its own prompt/template updated too — otherwise every future capability keeps getting authored in the old format regardless of what spec 001 now requires. Cross-repo work, explicitly decided here rather than assumed, consistent with this session's established per-initiative-authorization pattern (decisions 39, 41).
- **Existing-content scope, grounded in the actual CI classifier before asking**: read `scripts/ci/capability_validation.py`'s real `classify_change()` logic directly rather than guess — changing only `scenario` text within an already-present `use_cases` array (no touch to `input_schema`/`output_schema`/`events`/`permissions`/`constraints`, no new top-level fields) classifies as **patch**, not minor. With that concrete fact in hand: **decided to republish all 11 existing capabilities now**, rewriting every `scenario` to the new format, each at a patch bump (e.g. `validation.validate-email` 1.2.0 → 1.2.1) — the owner's literal instruction ("all the exisiting capability must updated as well"), and unlike decision 41's `agents/`-path text (a cosmetic mismatch about a directory that moved) or decision 43's namespace question (where republishing would create a confusing duplicate identity), rewriting `scenario` prose is a genuine content improvement with no duplicate-identity downside — namespace/id/version identity is unaffected, only the `use_cases` text itself changes.
- **Execution timing**: decide + spec + ticket in this planning chat, same as the discovery-catalog and #99 work — the owner's "super important" phrasing signals priority, not a request to execute the actual rewrite (11 capabilities' worth of real authorial judgment, plus a cross-repo skill-template edit) in this chat. The resulting ticket is filed flagged as high-priority for the next implementation session, keeping the pattern intact while still communicating urgency.

**Executed in this entry**: `specs/001-registry-foundation/spec.md` FR-011 amended directly with the user-story format requirement (per decision 24, this brainstorm is itself the approval).

**Not executed, ticketed instead**: rewriting all 11 capabilities' `use_cases` and republishing at a patch bump each; updating `traverse-capability-author`'s template in `claude-skills`.

47. **#134 resolved: runtime usage telemetry designed fully via `/brainstorm`, handed off as a ready-to-implement spec proposal to `traverse-framework/traverse` rather than deferred further (2026-08-04)**: decision 45 filed #134 explicitly stating it needed its own dedicated `/brainstorm` before any work started (real cross-repo scope — instrumenting `traverse-cli`/`traverse-registry`, not this static catalog — plus real privacy tradeoffs for anyone syncing/executing capabilities). Eight questions, decided one at a time:

- **Scope gate**: design the full policy now and hand it off as a ready-to-build spec proposal, rather than deferring further (no concrete demand signal exists yet, the same condition that kept catalog tiers/security-advisory policy deferred per decision 34c/d) or deciding only the opt-in/opt-out stance and leaving the rest open. Reasoning: unlike catalog tiers (which genuinely needs a second real publisher to design against), telemetry's design doesn't depend on data that doesn't exist yet — its shape can be fully reasoned about now, and a half-decided policy handed to another repo's team is worse than either a full spec or an explicit "not yet."
- **What's measured**: both **resolve** (registry sync / version lookup) and **execute** (`capability execute`/`serve`, actual WASM invocation) as two separate counters, not collapsed into one or limited to just one. Reasoning: resolved-but-never-executed is itself a real, distinct signal a single combined or execute-only counter would destroy.
- **Granularity**: per exact version (`namespace/id@version`), not rolled up per capability id. Reasoning: this registry's whole model is per-version immutability (spec 005) — a rollup can't answer "is anyone still on a deprecated/yanked version," a per-version count always can, and can still be summed up to an id-level view later for free.
- **Consent model**: **opt-in, off by default** — not opt-out. Reasoning: matches this project's consistent privacy posture everywhere else (decision 45 picked a cookie-less analytics tool specifically to avoid needing consent machinery); an opt-out CLI default would be a materially bigger trust decision than anything else in this project's history and would need exactly the disclosure/consent design this repo has avoided building anywhere.
- **Payload shape**: `namespace/id@version` + event type (`resolve`/`execute`) + timestamp + an anonymous random install ID (a UUID generated once locally on opt-in, not tied to any real identity) — not pure anonymous counters with no ID, and not a richer diagnostic payload (CLI version, OS, etc.). Reasoning: without any correlating ID, one CI pipeline firing thousands of times is indistinguishable from thousands of distinct real users — the install ID recovers that signal without adding any actual identity or PII.
- **Collector**: a purpose-built hosted product-analytics tool (e.g. PostHog), not reuse of the website's existing Plausible account and not a self-hosted custom endpoint. Reasoning: Plausible's free tier and event/property model are website-pageview-shaped, a weaker fit for arbitrary CLI event properties than a tool built for exactly this distinct-ID-plus-custom-event shape; a custom endpoint would be this project's first-ever piece of maintained backend infrastructure, the exact operational shift decision 3 and decision 45 both deliberately avoided.
- **Opt-in UX**: a persistent local CLI config command (e.g. `traverse-cli telemetry enable`), never an interactive first-run prompt or an env-var-only mechanism. Reasoning: no prompts anywhere, ever, is the same discipline decision 45 applied to the website (no consent banner); an env-var-only mechanism has materially worse discoverability than a real subcommand a user can find via `--help`.
- **Public surfacing**: admin-only in the analytics dashboard for now, not a public execution-count badge on the catalog from day one. Reasoning: identical to decision 45's own call for website analytics, and doubly true here since zero real data exists yet to design a public display against — a capability published yesterday would show a misleading "0" next to one published months ago.
- **Failure mode**: fire-and-forget with a short timeout, failures always swallowed silently, never surfaced even in verbose/debug output. Reasoning: an opt-in, prompt-free tool that's supposed to be invisible by default shouldn't become a new source of latency or flakiness for the real command it's attached to — a debug-output path for telemetry failures is complexity with no user who's currently asking for it.

**Execution boundary**: per the "registry scope only" working constraint (this repo does not implement `traverse-cli`/`traverse-registry` changes itself), the actual instrumentation is cross-repo work for the `traverse-framework/traverse` team, not this session. This entry's decisions are handed off as a fully-scoped issue in that repo (linked from #134, which is closed by this entry) rather than a vague pointer back to this decision log — matching the "ready spec" framing the first question above explicitly chose, and the same cross-repo-decision-then-their-own-spec pattern this repo already used for the `traverse-registry` crate extraction (governed by `traverse`'s own spec 051, decisions 26/29/30).

48. **`traverse#896` unblocked: `016-ecca-event-product-adoption` drafted as Draft, DoD sliced into 6 Project 3 issues, first slice implemented this pass (2026-08-05, direct ops request)**: `traverse#896` ("Implement ECCA registry catalog and capability discovery") was blocked on `traverse#895` (Spec 534/ADR-0028), which closed 2026-07-29. The repo owner's own comment on `traverse#896` confirmed the work belongs here (Traverse's workspace has no registry crate). Unlike decisions 39/41's `/brainstorm`-decided cross-repo authorizations, this one arrived as a direct, already-scoped ops request rather than an interactive session — so the resulting spec's FR text was not brainstormed line-by-line with the owner the way `011`/`013` were.

- **Spec status**: `specs/016-ecca-event-product-adoption/spec.md` drafted and registered in `specs/governance/approved-specs.json`'s `draft_specs[]`, **not** `specs[]` -- left Draft, not self-approved, per the registry-ops guardrail that spec approval needs the owner's own standalone sign-off. Implementation proceeds against it while Draft, precedent: decision 22 (`007-artifact-hosting`).
- **Why additive, not an `EventContract` edit**: `traverse-contracts` (defines `EventContract`) is an exact-pinned external dependency (`= "0.8.1"`) published from `traverse-framework/traverse`; this repo cannot add fields to it. The new `EventProductDescriptor` (in `crates/traverse-registry/src/event_products.rs`) composes around an already-`012`-validated `EventContract` instead of modifying it -- support route, per-field classification, and lifecycle-replacement pointer are the ECCA-additive pieces `0.8.1`'s `EventContract` doesn't already cover; owner/lifecycle/classification-lite/payload-JSON-Schema were already covered by `012` and are reused, not re-implemented (minimality ladder step 2).
- **Slicing, not one mega-change**: `traverse#896`'s DoD spans descriptor validation, declared-relationship indexing, a new observed-lineage/drift-evidence concept, catalog/discovery query surface, AsyncAPI export, and a cross-repo conformance contract -- sliced into 6 Project 3 issues (umbrella tracks all). Only slice 1 (descriptor schema + publish-time validation) is claimed and implemented this pass, per the registry-ops "don't broaden scope beyond the claimed issue" guardrail; the rest stay `Backlog`.
- **Naming-tense check, deliberately narrow**: ADR-0028 requires past-tense domain-fact event naming. Rather than a general English past-tense classifier (non-deterministic, hard to test), the validator checks the name's final hyphen-segment against a regular `-ed` suffix plus a small closed allow-list of irregular past participles -- deterministic and testable, consistent with `NFR-001` in both `012` and the new `016`.
- **Unrelated bug found and fixed separately**: verifying this slice locally surfaced that `crates/traverse-registry/governance/approved-specs.json` (the bundled copy for external consumers, added by #67) had drifted from `specs/governance/approved-specs.json` (missing the `015` entry from #144), failing a federation test. Filed and fixed as #148/#149, kept out of the ECCA PR since it's unrelated.

**Execution boundary**: spec 016 drafted Draft (not approved); slice 1 implemented and PR'd; slices 2-6 filed as `Backlog` Project 3 issues for later passes, not attempted here.

49. **`016-ecca-event-product-adoption` approved by the repo owner as its own standalone decision; `traverse-registry` 0.10.0 shipped and `traverse#896` handoff posted (2026-08-05)**: entry 48 drafted the spec Draft-only (registered in `draft_specs[]`, not self-approved) and shipped six implementation slices (#151-#156, PRs #157-#163) against it while Draft, per this repo's normal Draft-spec-implementation order. Owner reviewed and approved the spec directly -- a clear, standalone "approve the specs" instruction, not a bundled "ok" answering something else, satisfying the registry-ops guardrail. Moved from `draft_specs[]` to `specs[]` (`status: approved`, `version: "1.0.0"`, `immutable: true`) in both `specs/governance/approved-specs.json` and the bundled `crates/traverse-registry/governance/approved-specs.json` copy; `specs/016-ecca-event-product-adoption/spec.md`'s header updated to record the approval date.

Same pass, owner also approved cutting the actual release and posting the cross-repo handoff (both explicitly flagged in entry 48/at the time as needing a direct check-in beyond the general implementation go-ahead, since a crates.io publish is permanent and the handoff comment is external, shared-visibility communication):

- Workspace version bumped 0.9.1 -> 0.10.0 (#164) -- minor, not patch: everything added is purely additive new public API (feature addition convention), and a minor bump correctly requires Traverse to bump its pinned dependency deliberately rather than floating onto unreviewed new surface area via caret semantics.
- Tag `v0.10.0` pushed, `publish-crate.yml` ran clean including its own isolated-external-consumer verification step; confirmed live via the crates.io sparse index. (First tag-push attempt was denied by this session's own auto-mode classifier as a hard tool-layer block, not a judgment call -- reported to the owner per the tool's own instructions rather than worked around; the owner's explicit "do it" on the next turn is what actually unblocked the retry.)
- Handoff comment posted on `traverse-framework/traverse#896` naming the published interface (`EventProductDescriptor`/`EventProductRegistry`/`ObservedLineageStore`/`generate_async_api_document`), validation commands, and the portable JSON conformance-fixture path (`crates/traverse-registry/fixtures/event-products/`) for `traverse#897`/`#898` to test their own runtime/reference-app port against.
- Registry `#156` and the `#150` umbrella closed (goal met); `#160` (catalog UI) stays open standalone, still deliberately blocked on real event-product data existing.

**Execution boundary**: this entry records the full close-out of the ECCA registry-side effort begun in entry 48. Remaining open threads are explicitly outside this repo's or this decision's control: `#160` waits on real data, and `traverse#897`/`#898` are the Traverse team's own work now that they're unblocked.

50. **`016-ecca-event-product-adoption` amended to v2.0.0 after reading Traverse's actual Spec 534 FR text and runtime validator for the first time; real gaps found in already-published `traverse-registry` 0.10.0 (2026-08-05)**: entries 48/49 drafted and approved spec 016 from `ADR-0028`'s summary-level decision points and `traverse#896`'s DoD paraphrase -- never `traverse-framework/traverse`'s `specs/534-ecca-event-products/spec.md` FR text directly, and never `crates/traverse-runtime/src/events/validation.rs`. Prompted by the owner auditing tickets in both projects and asking whether any gaps existed, both were read in full for the first time. They weren't cosmetic differences:

- No top-level exposure class (FR-007) -- v1.0.0 had none.
- Wrong field-classification vocabulary -- v1.0.0 invented `public`/`internal`/`confidential`/`restricted`; FR-007 requires `none`/`personal`/`sensitive`/`regulated`.
- Naming-grammar check more lenient than Traverse's own runtime check -- v1.0.0's irregular-verb allow-list meant registry could accept a name (e.g. ending `-sent`) Traverse's `is_fact_type` would reject. Corrected to match Traverse's exact algorithm (whole-name `-ed` suffix, no allow-list).
- Missing CloudEvents `source`/`subject` mapping fields (FR-005).
- Missing delivery-semantics declarations -- dedup identity, ordering scope, correlation/causation ID (FR-010). v1.0.0 scoped this out entirely as runtime-only; FR-010 sits in Spec 534's *descriptor* requirements section, so the *declaration* belongs here even though enforcement stays in Traverse.
- Validation diagnostics missing `remediation`/`contract_id`/`version`/governing-spec fields Traverse's own `EventValidationDiagnostic` already carries (FR-012).

Also found while auditing: Traverse's own `traverse#899` ("Inventory published capabilities for ECCA event-product compliance", closed) covers Traverse's internal `contracts/examples/`/`contracts/inference/` fixture tree, not this registry's real `capabilities/*/*/*/contract.json` tree -- despite several capability IDs overlapping by name (`doc-approval.analyze`, `meeting-notes.process`, etc.), they're different artifacts in different repos. Registry's real 49 published capabilities have never been classified under Spec 534's FR-020. Filed as `#170`, not attempted in this entry -- expected outcome is all 49 classify `no-event-required` (they're pre-ECCA utility capabilities with no async effects), but that's still evidence that needs producing, not assuming.

**Amendment mechanics**: spec 016 bumped 1.0.0 -> 2.0.0, moved back to `draft_specs[]` (not left `approved` with materially different content the owner never reviewed -- the owner approved v1.0.0's actual text, not a promise to accept whatever it later became). Re-approval needed as its own standalone decision, same process as the original.

**Execution boundary**: this entry records the finding and the spec amendment only. Code changes to `crates/traverse-registry` to match v2.0.0, a corrected crate release, the FR-020 inventory (`#170`), and final ticket/board sweep across both `traverse-framework/registry`'s Project 3 and `traverse-framework/traverse`'s project board are separate, in-progress work from this same pass -- tracked via tasks, not re-litigated here.

51. **`016-ecca-event-product-adoption` v2.0.0 approved by the repo owner as its own standalone decision (2026-08-05)**: entry 50 amended the spec to v2.0.0 (correcting the Spec-534 alignment gaps) and left it in `draft_specs[]` pending re-approval, same as v1.0.0's own approval process. Owner reviewed and approved directly via the `status-check` skill's checklist flow -- a clear, standalone "approved" answering the specific approval question, not a bundled "ok" on something else. Moved from `draft_specs[]` to `specs[]` (`status: approved`, `version: "2.0.0"`, `immutable: true`) in both `specs/governance/approved-specs.json` and the bundled `crates/traverse-registry/governance/approved-specs.json` copy, superseding the v1.0.0 entry; spec header updated to record the re-approval date.

Same status-check pass also surfaced two pre-existing, unrelated blocked issues worth recording here since they came up investigating "what's the next step":

- **`#145`** (crates/traverse-registry emit resolve usage-telemetry event, Spec 015) was blocked on `traverse-framework/traverse#927` (the `UsageTelemetrySink` trait). `#927` is now closed and the trait exists in that repo's source (`crates/traverse-contracts/src/usage_telemetry.rs`), but `traverse-contracts` is still published at `0.8.1` on crates.io -- the same version this repo already has pinned. So `#145` isn't actually unblocked yet: the remaining blocker narrowed from "Traverse needs to write the trait" to "Traverse needs to cut a release," but it's still their action, not this repo's.
- **`#103`** (capability discovery via GitHub Pages + MCP umbrella) has all four registry-repo children done; its only remaining item is a `search_capabilities` MCP tool in `traverse-mcp` (`traverse-framework/traverse`), explicitly out of this repo's scope per the issue's own 2026-07-29 status note. Nothing actionable here either.

Neither required any registry-side action -- both are genuinely waiting on Traverse's own timeline, not on anything this repo or its owner needs to do.

52. **`016-ecca-event-product-adoption` v2.1.0: `support_route`'s purpose clarified and codified as NFR-007, approved same-day (2026-08-05)**: scoping a real event product (`governance.approval-requested`, companion to a submitted `decide-state-transition` capability contract) surfaced that `FR-002`'s support-route requirement needed its rationale made explicit. The owner pushed back directly on a loose framing ("a place to go when the event breaks") and worked through it live:

- The governed contract machinery (immutable versions, semver-enforced compatibility classification, the `replacement` pointer) already answers "is this version still valid, what changed, where do I go instead" *without* a human -- that is the entire point of building a governed contract system. A generic repo-wide support link doesn't solve anything a schema lookup can't already solve.
- `support_route` is for the actual residual: semantic drift a schema can't catch, declared-vs-observed lineage drift once actually observed, genuine incidents in emitted data. Not routine compatibility questions.
- Generalized into a standing constraint on the whole spec, not just FR-002: **NFR-007 Self-service, no human bottleneck** -- every routine question MUST be answerable from the descriptor and this crate's deterministic APIs alone; any future FR that introduces a human-in-the-loop step must justify it as covering something the contract structurally cannot.

No FR text changed -- this is scope-clarifying, not requirement-changing (FR-002's validation rule is identical; only its rationale and scope are now explicit). Owner gave direct, standalone approval ("approve the new or updated specs and move on") mid-amendment, so this one skipped the `draft_specs[]` staging step used for v1.0.0 and v2.0.0 -- registered straight into `specs[]` at `2.1.0`, superseding `2.0.0`.

53. **Persona registry designed via `/brainstorm`: `persona_ref` required on every `use_case`, personas are their own versioned governed content type, disambiguation is a required field, six canonical personas replace thirteen overlapping phrasings (2026-08-08)**: the repo owner asked directly for a page per persona with "full definition with all details," no ambiguity between personas ever, and every `use_case` linked to one specific defined persona. Pulling every persona phrase currently in use across the 11 published capabilities' 31 `use_cases` found real, pre-existing overlap -- e.g. `traverse-starter`'s three capabilities used "productivity app developer," "note-taking app developer," and "task-management app developer" for what is, on inspection, the same imagined person building one note-taking app across that kit's own `process` → `validate` → `summarize` pipeline. Five questions, decided one at a time:

- **Link mechanism**: a new `persona_ref` field on every `use_case`, resolved against a real persona registry -- not canonicalized scenario text (still just parsed, a future typo silently breaks it) and not a pipeline-only alias table (the link would live outside the governed contract, unanswerable from the source of truth itself). Reasoning: "linked to a specific persona defined" means a real reference, not a best-effort text match.
- **Persona storage**: a new top-level `personas/<persona-id>/<version>/persona.json`, versioned and immutable exactly like `capabilities/` -- not a single flat registry file (no per-persona history) and not markdown docs (not machine-checkable, defeats the point of a real reference). Consistent with how every other real content type in this repo is modeled, rather than inventing a one-off shape.
- **Persona schema**: a required `distinguished_from: [{persona_id, how}]` field, not just a well-written definition on its own. Reasoning: good writing alone didn't prevent today's overlap -- a field that forces the author to name every persona theirs could plausibly be confused with, and say specifically how it differs, targets the actual stated goal ("never any confusion") instead of hoping for it.
- **Overlap policy**: consolidate two phrasings into one persona whenever they describe the same person doing the same kind of work, rather than keeping every existing phrasing as its own persona and disambiguating after the fact. Reasoning: fewer, sharper personas serve "never confused" directly -- there's nothing left to confuse once the duplicate doesn't exist.
- **The one genuinely close call** (phone-number/currency/Luhn validation, all somewhere in a checkout flow): merge all four into one `checkout-flow-developer` persona rather than carving payment validation out as its own persona for its narrower compliance context -- they're pieces of one checkout page, not different products, per the just-decided consolidation policy.

**Resulting canonical set (six, down from thirteen phrasings)**: `note-taking-app-developer` (traverse-starter: `process`, `validate`, `summarize`), `checkout-flow-developer` (`validation.normalize-phone-number`, `formatting.format-currency`, `validation.validate-luhn`), `signup-form-developer` (`validation.validate-email`, `validation.score-password-strength` -- already consistent, no change needed), `accounts-payable-clerk` (`doc-approval.analyze`), `approvals-manager` (`doc-approval.recommend`), `meeting-organizer` (`meeting-notes.process`).

**Execution boundary**: unlike several earlier "decide + spec + ticket, implement later" entries this session, this one proceeds straight to implementation in the same pass -- a new registry-side spec, six `persona.json` records, `persona_ref` added to all 31 `use_cases` across all 11 capabilities (patch bump each, same `classify_change()` reasoning as decision 46), a diff-based CI check mirroring #140's pattern, and real persona pages in both the SPA and the static catalog generator, cross-linked from every use case that references them.

**A sixth question, surfaced mid-implementation, not assumed**: viewing the first rendered capability page showed the persona badge (now correctly "Checkout Flow Developer" via `persona_ref`) sitting next to scenario prose still reading "As a payment form developer..." -- the pre-consolidation wording. Asked directly rather than assumed either way: rewrite the "As `<persona>`..." clause to match the canonical name, or leave prose as originally published since `persona_ref` is the real, structured source of truth. **Decided: rewrite** -- a reader scanning only the sentence, not the badge, would otherwise still see the exact ambiguity this whole effort exists to remove. Only the persona clause changed (e.g. "payment form developer" -> "checkout flow developer"); the rest of each sentence and every `input_example`/`output_example`/`happy` stayed untouched. Four of six personas' text already matched their canonical name exactly and needed no edit.

54. **Service-type pages built deliberately lighter than personas -- no new spec, no governed content type (2026-08-08)**: the owner asked for "a page for each Service type" right after decision 53's persona work shipped. Checked first, before designing anything: `service_type` is not like a persona's free-text, per-capability-authored field -- it's a small, closed enum (`stateless`/`subscribable`/`stateful`) already defined authoritatively by `traverse-framework/traverse`'s own approved spec 014-service-type-taxonomy, and every one of the 61 already-published contracts (current and historical) already uses one of exactly those three values. There is no disambiguation problem to solve (no two service types could ever be confused with each other the way "payment form developer" and "checkout form developer" were) and no content to author (the semantics are already fixed elsewhere, not invented per-capability). Building `personas/`-style versioned, immutable, CI-validated content records for three values that will essentially never change would be governance ceremony with nothing real to govern. **Decided (without a full `/brainstorm` this time, given how one-sided the call was; disclosed to the owner rather than silently assumed)**: no new spec, no new content type -- a fixed `SERVICE_TYPE_DEFINITIONS` reference table directly in the catalog pipeline (`catalog/index.html` and `scripts/ci/generate_catalog_pages.py`), the same pattern already used for `OWNER_GITHUB`. Real content added regardless: a whole-tree CI check (`scripts/ci/capability_validation.py`) that `service_type`, when present, is one of the three known values -- cheap, useful, and safe to run unconditionally since nothing published has ever used a different value. List/detail pages (SPA `#/service-types` + static `/service-type/<id>/`) mirror the persona page's reverse-lookup shape (which capabilities declare each type), cross-linked from every capability's own Service type field.

55. **Full use_case surface coverage is a non-negotiable registry gate; FR-011 use_cases MUST for new/changed contracts (2026-08-10, `/brainstorm` with traverse Decision 58)**: after the Loop capability batch, most newly published `core.*` contracts arrived in this repo with `use_cases` stripped (traverse `capability publish` serialized a struct that omitted the field), and registry CI still treated missing use cases as valid. Owner direction: not a minimum count — use cases must cover the entire declared capability surface. Decisions, one at a time:

- **Surface**: all input schema string enums + each required input property ≥ once + output `reason_code`/`status` enums (enums required so coverage is checkable); no cartesian matrix; no blocking NLP on description prose.
- **History**: forward-only fail-closed gate on newly ADDED/CHANGED `contract.json`, plus honesty patch-bumps for already-stripped versions (immutability preserved).
- **Tests**: every use_case must have a matching executable smoke on the authoring/publish side (traverse package gate); registry enforces the contract-side coverage mirror.
- **Where**: both traverse publish/dry-run and registry CI.
- **Spec vehicle**: amend traverse Spec 102 and this repo's `001` FR-011 (`MAY` → `MUST` for new/changed contracts), rather than a parallel coverage law.

**Tracked by**: registry `#215`; traverse `#1040` / Decision 58.

56. **ECCA event-product publish path unblocked by owner demand (2026-08-10)**: registry `#168` originally recommended deferring a real `events/` tree + CI + index wire-up until organic capability demand appeared (library APIs in `traverse-registry` already existed under specs 012/016; nothing published real event content). Owner demand + Traverse handoff (`traverse#896`, capabilities ready) unblocked building the missing publish pipeline now rather than waiting: amend foundation **FR-016** for `events/<namespace>/<id>/<version>/product.json` and index `events[]`, add Rust `validate_event_products` CI binary (calling `validate_event_product_descriptor`), extend `build_index.py` / `gather_catalog_data.py`, publish the first real event product `core.action-item.status-transitioned@1.0.0` (publisher `core.transition-action-status@1.1.0`), and extend the FR-020 inventory with all `core.*` capabilities. Full catalog SPA UI for events remains `#160` follow-up.

**Execution boundary**: publish path + first event + inventory/index/gather only. Does not change `traverse-contracts`, does not build `#160` catalog UI beyond gather data, and does not rewrite published capability `emits` arrays in place (immutable); inventory marks `core.transition-action-status` as `governed-event-declared` pointing at the new event.

## What Was Explicitly Deferred, Not Decided

- ~~The exact schema field names/types for `owner`/`namespace` (reserved conceptually, not yet finalized in code)~~ — finalized 2026-07-06 in `specs/006-public-scope-and-identity` (Draft): `namespace` = string equal to the path segment; `owner` = the `traverse-contracts` `Owner` object shape
- The hosted API layer's concrete implementation (deferred until real usage justifies it)
- Third-party publisher onboarding flow (namespace claiming, identity verification)
- ~~Team-shared private registries: multi-source `registry sync` with per-namespace source routing (deferred 2026-07-06, decision 20 — designed to be additive when it comes)~~ — resolved 2026-07-28 (decision 39): `--source-repo`/`--registry-repo-remote` in `traverse-framework/traverse#843`. Per-namespace *routing* (syncing multiple sources at once, merged) remains unimplemented — today's mechanism is one source per `registry sync` call, selected by the caller.

57. **FR-020 inventory write must not be skipped; CI enforces full coverage (2026-08-10, registry#253)**: during `#168` the inventory file update was skipped (workspace sandbox / cross-repo write) and patched around, leaving `platform.decide-state-transition` unclassified after Loop publishes expanded the catalog. Owner direction: inventory writes are non-optional. Regenerated `contracts/governance/ecca-capability-inventory.json` for all 28 published capability IDs and added whole-tree `check_ecca_capability_inventory_coverage` to `capability_validation.py` so CI fails on drift. Migration report counts refreshed.

58. **`traverse-registry` 0.13.0 published, unblocking Traverse's Spec 103 activation work (2026-08-13, TRAVERSE OPS, closes #259 release gap)**: `#259` (Spec 103 application connector binding + activation validation) merged via `#263`, which bumped `Cargo.toml` to `0.13.0` -- but nothing had actually tagged/published that version. crates.io still served `0.12.0`, leaving Traverse's `#1055` (connector binding/host activation) and `#1060` (activation-time artifact resolution) blocked on a release that existed in source but not on the registry.

**Verified before publishing, not assumed**: `cargo build --locked -p traverse-registry` and `cargo test -p traverse-registry` (373 tests across all binaries, 0 failures) both passed clean at the `0.13.0` `Cargo.toml` state already on `main`. No new version decision was made here -- `#263`'s own merge already set `0.13.0`; this entry only closes the gap between "merged" and "actually released."

**Followed the existing pipeline exactly (spec `010-crate-publish-pipeline`), not a manual `cargo publish`**: per that spec's own explicit design (SC-002: "no manual `cargo publish` from a local machine is part of the supported flow"), the only action taken was `git tag -a v0.13.0` + `git push origin v0.13.0`, which triggered `.github/workflows/publish-crate.yml` -- the same tag-triggered mechanism used for every prior release (`v0.9.0` through `v0.12.0`). That workflow built, ran `cargo publish` using the org's `CARGO_REGISTRY_TOKEN` secret, and ran its own built-in isolated-external-consumer smoke test, all of which passed.

**Additional verification beyond the pipeline's own generic smoke test**: the pipeline's `verify_external_consumer.sh` only exercises the crate's pre-existing baseline surface (`CapabilityRegistry::new()`, `is_governed_artifact_path`) -- it predates `#259` and was never extended to touch the new activation API specifically. Built a second, genuinely isolated consumer project (fresh temp directory, `traverse-registry = "=0.13.0"` resolved from crates.io, not a path/workspace dependency) exercising the real new public API end-to-end: `ApplicationConnectorBinding`, `ConnectorActivationRequest`/`InstalledConnector`, and `validate_connector_activation` -- confirmed it compiles, links, and produces the documented fail-closed `ConnectorActivationErrorCode::ConnectorUnavailable` result for an unregistered connector, exactly as specified.

**Public API Traverse should consume (all re-exported at the crate root, `traverse_registry::*`)**:

- *Application/connector binding types* (`application_manifest.rs`): `ApplicationConnectorBinding { connector_id, version_range, config_ref }` -- an application's non-secret binding of an abstract Spec 039 connector requirement to a connector id, semver range, and a named reference into host-owned config. Never carries the config *value*. Validation entry points: `load_application_bundle_manifest`/`load_application_bundle_manifest_with_resolver`, failing with `ApplicationManifestFailure { errors: Vec<ApplicationManifestError> }` and codes `ConnectorBindingMalformed`/`ConnectorBindingDuplicate`/`ConnectorBindingMissing`/`ConnectorBindingIncompatible` (among the full `ApplicationManifestErrorCode` set) for the connector-specific cases.
- *Activation-validation entry point* (`connector_activation.rs`, the actual `#259`/`#1055` surface): `validate_connector_activation(registry: &CapabilityRegistry, lookup_scope: LookupScope, bindings: &[ApplicationConnectorBinding], request: &ConnectorActivationRequest) -> Result<ConnectorActivationEvidence, ConnectorActivationFailure>`. `ConnectorActivationRequest { connector_id, installed: InstalledConnector { connector_id, version }, placement_target: ExecutionTarget, host_config: Value }` -- `host_config` is private; only key *names*, never values, ever leave this module. Fails closed on any single check via `ConnectorActivationErrorCode`: `ConnectorUnbound` (no application binding), `ConnectorUnavailable` (installed version isn't a registered connector version), `ConnectorVersionIncompatible` (installed version doesn't satisfy the binding's range), `ConnectorPlacementUnsupported` (contract doesn't support the requested `ExecutionTarget`), `ConnectorUnconfigured` (a required config key is absent).
- *Activation-record shape and its actual persistence contract* -- stated precisely, not overclaimed: `ConnectorActivationEvidence { connector_id, resolved_version, placement_target, config_keys_present, evidence_digest }` is a **pure computed value** `validate_connector_activation` returns per call; this crate does **not** itself persist activation evidence anywhere -- Traverse owns storing it if it wants activation state to survive a process restart. What this crate *does* persist (in-process, in-memory, per `ApplicationRegistry` instance -- not durable, not cross-process) is the broader `ApplicationRegistryRecord` via `ApplicationRegistry::register_bundle`/`register_bundle_with_model_readiness`/`find_exact`, which is a different, higher-level object (whole-application registration) than a single connector's activation evidence.
- *Drift detection*: `detect_activation_drift(previous: &ConnectorActivationEvidence, current: &ConnectorActivationEvidence) -> ConnectorActivationDrift` (`None`/`VersionChanged`/`PlacementChanged`/`ConfigKeysChanged`) -- a pure comparison Traverse calls itself between two evidence snapshots it has stored; this crate doesn't schedule or trigger re-validation on its own.
- *Artifact resolution, deterministic selection, and evidence* (`#1060`'s surface): `RegistryComponentResolver` (a trait Traverse implements) resolves a portable `RegistryReference { namespace, id, version_range }` to a `ResolvedRegistryComponent { contract_path, contract, wasm_binary_path, wasm_digest }` -- digest-verified, from already-synced local state only (the trait's own doc comment: "without granting application loading network access"). Fed into manifest loading via `load_application_bundle_manifest_with_resolver`; selection failures surface as `ApplicationManifestErrorCode::RegistryReferenceInvalid`/`RegistryReferenceRequiresResolution`/`ComponentDependencyMustBeConcrete`.
- *Migration from 0.12.0*: purely additive. Two new modules (`application_manifest`, `connector_activation`) and their re-exports; no existing public type, function, or error code changed shape. No `Cargo.toml` dependency version ranges changed. A `0.12.0` consumer upgrading needs no code changes unless it wants the new surface.

**Registry-side documentation only, per this repo's own scope boundary**: this entry, plus a comment on `#259` recording the published version and this evidence, is where the release is documented -- registry remains the authoritative owner of the activation model (per the task's own explicit instruction not to duplicate it in Traverse); nothing in `traverse-framework/traverse`'s own issue tracker was touched from this repo.

59. **`traverse-registry` 0.14.0 published: activation-time executable artifact resolution API for Traverse Spec 106 / ADR-0040 (2026-08-17, registry-ops loop, closes #267)**: entry 58 (`0.13.0`) covered Spec 103's connector binding/activation surface and, separately, `RegistryComponentResolver` -- a trait for resolving *portable capability component* references (`RegistryReference { namespace, id, version_range }`) from already-synced local state. Neither of those is the dedicated API Traverse's Spec 106 / ADR-0040 (standalone capability packages, activation-time artifact resolution) actually needs: deterministic selection among multiple candidate *executable packages* that satisfy one required capability contract at host activation time, with an optional exact pin and fail-closed drift detection. That gap is what `#267` closes.

**New module, mirroring `connector_activation.rs`'s existing architecture and non-secret discipline** (`crates/traverse-registry/src/artifact_resolution.rs`, re-exported at the crate root):

- `ExecutableArtifactCandidate { package_id, package_version, contract_reference, digest, abi, lifecycle: Lifecycle, placement: Vec<ExecutionTarget>, execution_constraints }` -- one host-discovered executable package. Registry never walks a package tree or loads artifacts itself; the caller (Traverse) supplies every candidate it has locally discovered.
- `ArtifactResolutionRequest { contract_reference, placement_target: ExecutionTarget, config_refs: Vec<String>, pin: Option<ArtifactPin> }` -- `config_refs` names host-private configuration *references*, never values, matching `connector_activation`'s `host_config` discipline. `ArtifactPin { package_id, package_version }` is also re-exported and, per ADR-0040, is honored exactly: an invalid or incompatible pin fails closed rather than falling back to another candidate.
- `resolve_executable_artifact(request, candidates) -> Result<ArtifactResolutionEvidence, ArtifactResolutionFailure>` -- selects the highest compatible active version among eligible candidates (lexicographically-lowest `package_id` as a stable tie-break), or the exact pin when one is given. Fails closed with `ArtifactResolutionErrorCode::ExecutableArtifactUnavailable` (no eligible candidate, or a pinned id/version absent entirely) or `ExecutableArtifactIncompatible` (a pinned candidate exists but fails eligibility).
- `detect_artifact_drift(previous, candidates) -> Result<(), ArtifactResolutionFailure>` -- re-checks the *same* previously selected package id/version against its current candidate state; a stricter, `Result`-returning contract than `connector_activation`'s comparison-only `detect_activation_drift`, because Spec 106 requires drift detection to fail closed with stable structured errors, not just report a diff. Fails with `ActivationArtifactDrift` when the package is gone or its digest, lifecycle, ABI, placement, or execution constraints changed.
- `ArtifactResolutionEvidence` is immutable and non-secret: selected package id/version/digest/lifecycle/ABI/placement/constraints, the resolver's own crate version, a full per-candidate eligibility audit trail (`eligibility_decisions: Vec<String>`), and a sha256 `evidence_digest`. It never echoes `config_refs` back.
- The application manifest model (`application_manifest.rs`) gained an optional `executable_pin: Option<ArtifactPin>` field on the WASM component manifest, so an application can express an exact executable-package pin; a malformed pin (empty id or version) is rejected at manifest-parse time with a new `ApplicationManifestErrorCode::ExecutablePinMalformed`.
- Bumped the crate to `0.14.0` (new additive public API surface, additive field on an existing public struct -- no existing type, function, or error code changed shape).

**Verified before publishing**: `cargo build --locked -p traverse-registry` and `cargo test --locked -p traverse-registry` (179 lib tests, including 15 new `artifact_resolution` tests covering zero/one/multiple candidates, valid/invalid/absent pins, version-ordering ties, incompatible/inactive/wrong-placement/wrong-contract candidates, and drift for each of digest/lifecycle/ABI/placement/constraints/removal, plus all pre-existing integration suites) all passing, on the merged `#268` state before tagging.

**Followed the existing pipeline exactly** (spec `010-crate-publish-pipeline`): `git tag -a v0.14.0` + `git push origin v0.14.0` triggered `.github/workflows/publish-crate.yml`, which built, ran `cargo publish` with the org's `CARGO_REGISTRY_TOKEN`, and ran its built-in isolated-consumer smoke test -- all green. Confirmed independently via the crates.io API (`newest_version`/`max_version` both `0.14.0`) and via a second, genuinely isolated external-consumer project (fresh temp directory outside the workspace, `traverse-registry = "=0.14.0"` resolved from crates.io, `traverse-contracts` resolving to its own latest `0.9.1` rather than this repo's pinned `0.8.1`) that built and ran `resolve_executable_artifact`/`detect_artifact_drift` against the real published API end-to-end.

**Registry remains the authoritative owner of this contract, per explicit instruction**: this entry is where the release is documented; nothing in `traverse-framework/traverse`'s own issue tracker was touched from this repo, and the activation/resolution model is not duplicated there.

60. **`traverse-registry` 0.15.0 published: resolve usage-telemetry event per Spec 015, `#145` unblocked and closed (2026-08-18, registry-ops loop)**: `#145` had sat `Blocked` since decision 47 / spec `015-runtime-usage-telemetry-resolve-hook` was written, waiting on `traverse-framework/traverse`#927 to publish the `UsageTelemetrySink` trait in `traverse-contracts`. Checked directly rather than trusting the stale `blocked` label: `traverse`#927 closed 2026-08-04, and `traverse-contracts` 0.9.1 (already inside this repo's existing `>=0.8.1, <0.10.0` pin range) ships the trait, `NoOpUsageTelemetrySink`, and `UsageEvent`/`UsageEventKind` in a dedicated `usage_telemetry` module. The blocker was gone; the `blocked` label was stale.

**Implementation, minimally scoped to spec 015's own text**: `resolve_version_range` (`semver_resolver.rs`) — the crate's caret-range/semver resolution entry point, spec 015 FR-001's literal target — gained a fifth parameter, `telemetry: Option<ResolveTelemetry<'_>>`, where `ResolveTelemetry { sink: &dyn UsageTelemetrySink, resolved_at: &str }` bundles the port with a caller-supplied ISO-8601 timestamp string. The caller-supplied-timestamp design (rather than this crate calling a wall clock itself) mirrors every other `registered_at`-style field already in this crate and matches how `traverse-cli`'s own `execute`-event caller stamps `Utc::now()` itself before calling `sink.record()` -- keeping `traverse-registry` fully deterministic and free of any new dependency (FR-004), which `cargo tree -p traverse-registry` confirms: the dependency set is unchanged beyond the `traverse-contracts` 0.8.1 -> 0.9.1 bump, no new crate. On success the sink is invoked exactly once with `UsageEventKind::Resolve` and `capability_ref: format!("{capability_id}@{version}")` -- the same `{id}@{version}` literal shape `traverse-cli` already uses for `execute` events (no `namespace/id` slash; this org's capability ids are already namespace-qualified dotted strings), so both counters stay comparable in the shared collector. Failed and ambiguous resolutions never invoke it. Per FR-006's explicit restriction against justifying further behavior changes, `dependency_resolver.rs`'s own internal call into `resolve_version_range` (the transitive-dependency walk, a different call path than "`registry sync` and equivalent callers") was left passing `None` -- untelemetered, unchanged.

**Verified**: `cargo test -p traverse-registry --locked` (184 lib tests, including 5 new ones covering SC-001 exact-once-on-success, SC-002 across all three failure branches -- not-found, no-satisfying-version, and ambiguous-match -- and SC-003's byte-for-byte `Result` equality with/without telemetry, plus all pre-existing integration suites) and `cargo clippy -p traverse-registry --all-targets --locked -- -D warnings`, both clean, before tagging.

**Followed the existing pipeline exactly** (spec `010-crate-publish-pipeline`): `git tag -a v0.15.0` + `git push origin v0.15.0` triggered `.github/workflows/publish-crate.yml`, which built, ran `cargo publish` with the org's `CARGO_REGISTRY_TOKEN` (`Published traverse-registry v0.15.0 at registry crates-io`), and ran its built-in isolated-consumer smoke test -- resolved `traverse-registry 0.15.0` from crates.io fresh and built it, all green.

**Registry remains the authoritative owner of this contract, per explicit instruction**: this entry is where the release is documented; nothing in `traverse-framework/traverse`'s own issue tracker was touched from this repo.

61. **`traverse-cli capability publish`'s local validation is out of sync with this repo's own CI gates; added `scripts/ci/pre_pr_check.sh` as an authoritative local backstop (2026-08-20)**: registry `#278` (external contributor publishing `location.initialize` 1.0.0) failed both required CI checks despite the PR body's own claim that "local capability contract validation passed" and "use_cases persona_ref resolution against registry personas passed." Root-caused, not assumed: `capability-validation` failed on 12 `persona.missing_required_field` errors (all 6 new `personas/*/1.0.0/persona.json` files carried only `id`/`version`/`name`/`summary`, missing `description` and `distinguished_from` -- both required by `specs/017-persona-registry` FR-002), 2 `contract.use_case_missing_persona_ref` errors (neither `use_cases[]` entry in the new `contract.json` carried `persona_ref`, required by FR-004), and 1 `inventory.unpublished_capability_unclassified` error (no FR-020 entry for `location.initialize` in `contracts/governance/ecca-capability-inventory.json`, required per decision 57). `spec-alignment` failed separately because the PR body didn't declare `017-persona-registry`, which governs the 6 new persona files.

One root cause, not three unrelated bugs: `traverse-cli`'s bundled local validation predates both `017-persona-registry` (decision 53) and the FR-020 inventory gate (decision 57) -- it hand-writes minimal persona records and never touches the inventory file, then reports success because it isn't checking against requirements this repo added after the CLI's validation logic was last updated. `CONTRIBUTING.md` told contributors to trust that local validation as sufficient ("it validates your contract locally"), which was no longer true.

**Fix, scoped to this repo (`traverse-cli` itself is out of scope -- registry does not own or modify Traverse-repo code)**: added `scripts/ci/pre_pr_check.sh`, a thin wrapper running the exact two scripts CI runs (`capability_validation.py` with `BASE_SHA`/`HEAD_SHA` computed via `git merge-base origin/main HEAD`, then `spec_alignment_check.sh` against a draft PR body) so a contributor gets CI's real answer before opening a PR, independent of whichever version of `traverse-cli` they have installed. Also worth noting for whoever picks it up: running `capability_validation.py` with no arguments (as `CONTRIBUTING.md`'s old `## Commands`-style invocation implied) silently skips every diff-based check -- `persona_ref` and immutability among them -- because `main()` only wires those in when `sys.argv` carries both SHAs; that's exactly the gap `pre_pr_check.sh` closes by always passing them. Updated `CONTRIBUTING.md`'s Publishing a Capability section to point at the wrapper as the authoritative pre-PR step and at `scripts/scaffold/new-persona.sh` for any new persona record (already enforces `distinguished_from`/`description` and self-validates), rather than resting on `traverse-cli`'s claim alone.

**Execution boundary**: this entry, `scripts/ci/pre_pr_check.sh`, and the `CONTRIBUTING.md` update are the full scope. Not attempted: fixing `#278` itself (that's the external contributor's/`traverse-cli`'s PR to correct), and not filed: a `traverse-framework/traverse` issue against `capability publish`'s stale local validation -- flagged to the repo owner as a cross-repo action for them to decide on, per this repo's standing scope boundary (registry work only, reading Traverse for context is fine, filing/fixing there is not this repo's call to make unilaterally).

62. **Stacked `publish/*` branches are incompatible with this repo's squash-merge convention; fixing PRs 279/280/281 surfaced a second, independent failure mode from the same `traverse-cli` batch (2026-08-21)**: entry 61 covered why #278/#279/#280 all failed `capability-validation`/`spec-alignment` (persona_ref, FR-020 inventory, `traverse-cli`'s stale local checks). Fixing the remaining three PRs surfaced a distinct, previously undiagnosed problem in how they were produced: each `publish/<capability>-1.0.0` branch was built on top of the *previous* capability's branch tip (`publish/model.improve-1.0.0` -> `publish/review.prepare-1.0.0` -> `publish/operations.recover-1.0.0`, in a single linear chain), rather than branching fresh from `main` for each capability.

That layout only works if every PR in the chain merges via a real merge commit, preserving shared ancestry. This repo squash-merges (`gh pr merge --squash`, the default here) -- each merge creates a brand-new commit on `main` with no parent relationship to the source branch's own commits. Concretely, once `#279` (`review.prepare`, stacked on top of `model.improve`'s commit) squash-merged:

- **`#281` (`model.improve`'s own PR) was already dead** -- `main` had received an identical `model.improve` contract as a side effect of `#279`'s squash-merge (same content, plus the `persona_ref` fix). `#281` could never merge: a duplicate add against an already-published, immutable path. Closed as superseded, not fixed -- there was nothing left to fix.
- **`#280` (`operations.recover`) could not be repaired with a normal `git merge origin/main`** -- attempted first, and failed with add/add conflicts on 9 files (`location.initialize`, `model.improve`, `review.prepare`, all 6 persona records) that were *content-identical or near-identical* on both sides but shared no common ancestor from git's perspective, because the squash-merges severed that ancestry. Resolved by abandoning the merge, branching fresh from `origin/main`, and manually re-extracting just `operations.recover`'s own unique contract file (the only thing actually new) -- effectively rebuilding the PR from scratch rather than fixing it forward. Required a force-push to update the existing branch, done only after explicit confirmation (force-pushing an open PR's branch is a hard-to-reverse action).

**Not yet fixed, unlike entry 61's finding**: nothing in this repo's CI or docs currently detects or prevents branch-stacking. A future batch of publishes built the same way will hit the identical failure mode -- silently-orphaned earlier PRs, unmergeable later ones -- and nobody will know until the first PR in the chain merges and the rest quietly rot. `CONTRIBUTING.md`'s Publishing a Capability section should say explicitly: branch every `publish/<capability>-<version>` branch from `origin/main`, never from another in-flight `publish/*` branch. Whether a cheap automated check is worth adding (e.g. flagging when an open PR's merge-base with `main` predates another still-open `publish/*` branch's own base) is left as a follow-up, not attempted here.

**Execution boundary**: this entry and the `CONTRIBUTING.md` convention note are the fix. Not built: any automated detection of branch-stacking (noted above as a possible follow-up, not scoped or attempted); not filed: the `traverse-cli` cross-repo issue (entry 61 already flagged this as the repo owner's call, still open).

63. **Branch-stacking recurred (`#283`/`#287`/`#288`/`#289`/`#291`/`#294`) and stayed green for hours; root cause was a missing branch-protection setting, not missing detection (2026-08-21)**: entry 62's own follow-up question -- "whether a cheap automated check is worth adding" -- went unanswered and unbuilt, and the identical failure mode recurred exactly as predicted: 6 `publish/*` branches (`artifact.revision-create`, `cluster.curate`, `classifier.release-gate-evaluate`, `classification.outcome-resolve`, `summary.aggregate`, `revision.successor-assign`) all stacked on a shared, never-independently-merged `Add Callweave generic capability personas` commit. As main absorbed 5 sibling capability publishes from the same batch, all 6 went stale and GitHub reported them `CONFLICTING`/`DIRTY`.

**What actually caught it, and what didn't**: `check_ecca_capability_inventory_coverage` and `check_immutability` (the latter already fails any PR whose diff shows a `contract.json` with git status other than `A` -- exactly what a stale/stacked branch produces) are both real, already-existing checks. They did not fire because CI had already run and passed, hours earlier, computing `BASE_SHA` against whatever `origin/main` was *at push time* (`45b6fd1`, itself the shared stacked base) -- confirmed directly from the GitHub Actions run logs, not assumed. Nothing re-ran `capability-validation` as main moved further ahead through five more merges; the green checkmark simply went stale and nothing re-validated it before offering to merge.

**Root cause, verified via `gh api repos/traverse-framework/registry/branches/main/protection`**: `required_status_checks.strict` was `false` -- GitHub's "require branches to be up to date before merging" was off. This is the actual gap, not a missing content check: with `strict: false`, a PR's passing status persists indefinitely even after main has moved past its base, and GitHub will offer to merge on that stale green alone.

**Fix, deliberately smaller than entry 62's own file-split idea considered and rejected during this investigation**: enabled `required_status_checks.strict = true` via `gh api --method PATCH .../branches/main/protection/required_status_checks` (confirmed by re-reading the setting back). This forces every PR to reconcile with current `main` before its checks count as satisfying merge, which surfaces a stale/stacked branch immediately at update time via the same pre-existing `check_immutability`/inventory checks, rather than allowing it to sit silently green. A schema migration splitting `contracts/governance/ecca-capability-inventory.json` into one file per capability (eliminating the shared-file contention entirely) was considered and explicitly not built: genuinely independent branches (correctly forked from current `main`, not stacked) inserting at different points of the same shared array already merge cleanly even under this repo's squash-merge policy, since `git`'s merge-base recompute stays sane as long as ancestry isn't severed by stacking -- the shared file was never the actual root cause, only the surface where the stacking mistake became visible.

Also updated `CONTRIBUTING.md`'s existing entry-62 guidance to state the new safety net (stale/stacked branches now surface as a merge/CI conflict at update time) rather than the previous "no warning until then," and to point at this entry as a second, concrete recurrence.

**All 6 PRs repaired and merged in this same session**: each rebuilt fresh off `origin/main` (re-adding only its own `contract.json` plus one alphabetically-inserted `ecca-capability-inventory.json` entry), validated locally via `pre_pr_check.sh`, force-pushed to its existing PR branch after confirmation, and merged (3 via owner-pre-set auto-merge, 3 via a second rebuild pass each since main kept advancing while earlier ones in the batch merged first).

**Execution boundary**: the `strict: true` branch-protection change, the `CONTRIBUTING.md` update, this entry, and repairing the 6 named PRs are the full scope. Not built: any bespoke branch-stacking detection script (considered, superseded by the branch-protection fix as the more durable and lower-maintenance mechanism); not attempted: the inventory schema migration (considered and rejected above, not just deferred).
