[workspace]

[package]
name = "validate-luhn-agent"
version = "0.1.0"
edition = "2021"
publish = false
description = "Real, deterministic, ABI-compliant (no_std) implementation of the validation.validate-luhn capability."

[[bin]]
name = "validate-luhn-agent"
path = "src/main.rs"

[dependencies]
wasi-agent-runtime = { path = "../wasi-agent-runtime" }

[profile.release]
opt-level = "s"
lto = true
panic = "abort"
